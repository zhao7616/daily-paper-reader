---
title: Retrieval-Augmented Generation with Covariate Time Series
title_zh: 协变量时间序列的检索增强生成
authors: "Kenny Ye Liang, Zhongyi Pei, Huan Zhang, Yuhui Liu, Shaoxu Song, Jianmin Wang"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04951v1"
tags: ["query:rul"]
score: 8.0
evidence: 工业阀门预测性维护的时间序列RAG框架
tldr: 本研究针对工业场景（如PRSOV维护）中时间序列数据稀缺、序列短且协变量耦合的挑战，提出了RAG4CTS框架。该框架是一种无需训练的检索增强生成方案，通过构建层次化原生知识库实现无损存储，并设计了双阶段双加权检索机制以对齐历史趋势。此外，引入代理驱动策略动态优化上下文。实验及南航实际部署结果证明，该方法在预测精度和故障识别方面表现卓越，实现了零误报。
selection_source: fresh_fetch
motivation: 现有的时间序列RAG方法在处理数据稀缺、短序列及复杂协变量耦合的工业预测场景时，难以有效区分相似状态。
method: 提出了RAG4CTS框架，利用层次化原生知识库、双阶段双加权检索机制和代理驱动的自监督上下文优化来增强预测。
result: 实验显示该框架在PRSOV预测精度上显著超过现有SOTA模型，并在实际航空维护部署中成功识别故障且无误报。
conclusion: RAG4CTS证明了结合物理感知检索与动态上下文优化的RAG范式在复杂协变量时间序列分析中的有效性与实用性。
---

## 摘要
尽管检索增强生成（RAG）极大地增强了大语言模型（LLMs），但将这一范式扩展到时间序列基础模型（TSFMs）仍然是一个挑战。这一点在调压切断阀（PRSOV）的预测性维护中得到了体现，这是一个高风险的工业场景，具有（1）数据稀缺、（2）短瞬态序列以及（3）协变量耦合动力学的特征。不幸的是，现有的时间序列 RAG 方法主要依赖于生成的静态向量嵌入和可学习的上下文增强器，在这些稀缺、瞬态和协变量耦合的场景中，可能无法区分相似的状态（regimes）。为了解决这些局限性，我们提出了 RAG4CTS，这是一个针对协变量时间序列的、状态感知的、无需训练的 RAG 框架。具体而言，我们构建了一个分层的时间序列原生知识库，以实现原始历史状态的无损存储和物理启发式检索。我们设计了一种两阶段双加权检索机制，通过逐点和多变量相似性来对齐历史趋势。对于上下文增强，我们引入了一种智能体驱动的策略，以自监督的方式动态优化上下文。在 PRSOV 上的大量实验表明，我们的框架在预测准确性方面显著优于最先进的基准模型。所提出的系统已部署在中国南方航空公司的 Apache IoTDB 中。自部署以来，我们的方法在两个月内成功识别了一次 PRSOV 故障，且零误报。

## Abstract
While RAG has greatly enhanced LLMs, extending this paradigm to Time-Series Foundation Models (TSFMs) remains a challenge. This is exemplified in the Predictive Maintenance of the Pressure Regulating and Shut-Off Valve (PRSOV), a high-stakes industrial scenario characterized by (1) data scarcity, (2) short transient sequences, and (3) covariate coupled dynamics. Unfortunately, existing time-series RAG approaches predominantly rely on generated static vector embeddings and learnable context augmenters, which may fail to distinguish similar regimes in such scarce, transient, and covariate coupled scenarios. To address these limitations, we propose RAG4CTS, a regime-aware, training-free RAG framework for Covariate Time-Series. Specifically, we construct a hierarchal time-series native knowledge base to enable lossless storage and physics-informed retrieval of raw historical regimes. We design a two-stage bi-weighted retrieval mechanism that aligns historical trends through point-wise and multivariate similarities. For context augmentation, we introduce an agent-driven strategy to dynamically optimize context in a self-supervised manner. Extensive experiments on PRSOV demonstrate that our framework significantly outperforms state-of-the-art baselines in prediction accuracy. The proposed system is deployed in Apache IoTDB within China Southern Airlines. Since deployment, our method has successfully identified one PRSOV fault in two months with zero false alarm.

---

## 论文详细总结（自动生成）

以下是对论文《Retrieval-Augmented Generation with Covariate Time Series》（协变量时间序列的检索增强生成）的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的时间序列基础模型（TSFMs）在处理高风险工业场景（如飞机的调压切断阀 PRSOV 维护）时存在能力鸿沟。
*   **研究背景**：工业场景具有三大挑战：
    1.  **数据稀缺**：关键运行状态（如起飞阶段的压力调节）极少，难以训练深度模型。
    2.  **短瞬态序列**：状态转换极快，如 10 秒内仅 18 个数据点，信息密度低。
    3.  **协变量耦合**：目标变量（如歧管压力 MP）受外部协变量（如发动机转速 N2、中间压力 IP）被动驱动，忽视物理逻辑会导致预测失效。
*   **现有 RAG 的局限**：现有的时间序列 RAG 依赖可学习的适配器（在稀缺数据下难以收敛）和静态向量嵌入（在短序列中引入噪声且丢失精度），且往往忽略了协变量的物理逻辑。

### 2. 方法论：RAG4CTS 框架
论文提出了一个**状态感知、无需训练**的 RAG 框架，核心包含三个组件：
*   **层次化原生知识库 (Hierarchical KB)**：
    *   采用树状结构（根-机型-机号-设备-传感器）存储原始序列，而非向量化嵌入。
    *   **核心思想**：实现无损存储，保留短瞬态序列的数值精度，避免分段或填充带来的误差。
*   **两阶段双加权检索机制**：
    *   **双加权逻辑**：结合“关键点权重”（优先考虑近期状态和未来控制输入）和“协变量权重”（利用互信息 MI 衡量协变量对目标的物理耦合程度）。
    *   **第一阶段（形状对齐）**：使用加权余弦相似度筛选出趋势一致的候选者。
    *   **第二阶段（状态精度）**：使用加权矩阵轮廓（Matrix Profile）距离精确定位数值最接近的参考段。
*   **智能体驱动的上下文增强 (Agent-driven Augmentation)**：
    *   **核心思想**：将检索到的 Top-1 样本作为“代理查询”，利用其已知的未来值作为真值进行自监督评估。
    *   **算法流程**：通过贪心搜索确定最佳的上下文拼接数量 $k^*$，从而在激活模型上下文学习（ICL）能力的同时避免引入过多噪声。

### 3. 实验设计
*   **数据集**：中国南方航空（CSA）提供的 PRSOV 真实数据集，涵盖 B777 和 A320 机型，分为左/右发动机四个子集。
*   **任务设置**：短线瞬态预测（输入 6 个点，预测未来 12 个点）。
*   **对比方法 (Benchmarks)**：
    1.  **深度学习模型**：DLinear, PatchTST, iTransformer, TimeMixer, TimeXer 等。
    2.  **零样本 TSFMs**：Chronos-2, TimesFM, Sundial 等。
    3.  **RAG 框架**：TS-RAG（基于嵌入的 SOTA 方法）。
*   **消融实验**：针对知识库范围、检索指标（DTW vs. MP）、加权方案、上下文长度 $k$ 进行了详细测试。

### 4. 资源与算力
*   **训练成本**：论文强调该框架是 **Training-free（无需训练）** 的，直接利用预训练的 Chronos-2 作为骨干模型，因此在部署阶段不需要昂贵的 GPU 训练算力。
*   **部署环境**：系统部署在 Apache IoTDB 的 AINode 上，支持通过标准 SQL 进行数据库内推理。
*   **具体算力说明**：文中未详细列出推理时的具体 GPU 型号或耗时，但提到其设计目标是最小化数据迁移开销。

### 5. 实验数量与充分性
*   **实验规模**：使用了 2023-2024 年超过 10 万条历史样本构建知识库，并在 2025 年的查询集上验证。
*   **充分性**：实验设计非常全面，不仅有标准的精度对比（MSE/MAE），还包含了协变量影响分析、不同机型间的知识迁移分析，以及长达两个月的实际生产环境部署验证。
*   **客观性**：通过与多种不同类别的 SOTA 模型对比，并提供了详细的消融实验来证明每个组件的有效性，实验结果具有较高的说服力。

### 6. 主要结论与发现
*   **性能卓越**：RAG4CTS 在所有测试子集上均显著优于现有模型，将零样本 TSFM 的预测误差降低了数倍。
*   **物理一致性**：通过引入协变量加权，检索到的参考段在物理逻辑上与查询样本高度一致。
*   **工业价值**：在南航部署的两个月内，成功识别出 1 次真实的 PRSOV 故障，且实现了**零误报**，证明了其在预测性维护中的可靠性。

### 7. 优点与亮点
*   **原生存储与检索**：摒弃了传统的向量嵌入，直接在原始数据空间操作，解决了短序列精度丢失的问题。
*   **物理启发式设计**：利用互信息和未来协变量信息进行加权，使 RAG 过程具备了物理感知能力。
*   **自适应增强**：引入智能体策略动态调整上下文长度，克服了固定超参数 $k$ 的局限性。
*   **落地性强**：与工业数据库（IoTDB）深度集成，并有实际的航空安全应用案例。

### 8. 不足与局限
*   **领域局限性**：该方法高度依赖于“状态（Regime）”的定义，适用于具有明显周期性或重复性操作模式的工业设备，对于完全随机或无规律的时间序列可能效果有限。
*   **检索开销**：虽然无需训练，但随着知识库规模的极度扩张，两阶段检索的计算延迟可能会成为实时预测的瓶颈（尽管文中采用了分层索引来缓解）。
*   **骨干模型依赖**：性能仍受限于底层 TSFM（如 Chronos-2）的上下文窗口长度和推理能力。

（完）
