---
title: Domain-Adaptive Health Indicator Learning with Degradation-Stage Synchronized Sampling and Cross-Domain Autoencoder
title_zh: 基于退化阶段同步采样与跨域自编码器的领域自适应健康指标学习
authors: "Jungho Choo, Hanbyeol Park, Gawon Lee, Yunkyung Park, Hyerim Bae"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10430v1"
tags: ["query:rul"]
score: 9.0
evidence: 用于预测与健康管理的健康指标学习
tldr: 针对工业设备在不同工况下健康指标建模存在的分布不匹配问题，本文提出了一种领域自适应框架。该框架通过退化阶段同步采样（DSSBS）解决随机采样导致的阶段失配，并利用跨域对齐融合大自编码器（CAFLAE）捕捉长程时间依赖。实验表明，该方法在多个数据集上显著提升了跨域对齐效果和监测性能，为长期工业状态监测提供了高效的解决方案。
selection_source: fresh_fetch
motivation: 现有的深度学习健康指标建模方法在处理变工况下的分布偏移时，面临退化阶段失配和长程时间特征提取能力不足的挑战。
method: 提出了结合核变点检测的同步采样策略（DSSBS）和集成大卷积核与交叉注意力的自编码器（CAFLAE）来学习领域不变表征。
result: "在韩国国防系统和XJTU-SY轴承数据集上的实验结果显示，该方法比现有最先进技术平均性能提升了24.1%。"
conclusion: 通过阶段一致性采样和高性能特征提取骨干网络，该框架有效解决了跨域健康监测中的对齐难题，具有显著的工业应用价值。
---

## 摘要
高质量健康指标（HIs）的构建对于有效的故障预测与健康管理（PHM）至关重要。尽管深度学习显著推动了健康指标建模的发展，但现有方法在应对由不同工况导致的分布不匹配问题时仍面临挑战。虽然通常采用领域自适应来缓解这些偏移，但仍存在两个关键挑战：（1）随机小批量采样过程中退化阶段的不对齐，导致产生误导性的差异损失；（2）小卷积核一维卷积神经网络（1D-CNNs）在捕获复杂振动信号中的长距离时间依赖关系方面存在结构性局限。为解决这些问题，我们提出了一个领域自适应框架，包括退化阶段同步批量采样（DSSBS）和跨域对齐融合大核自编码器（CAFLAE）。DSSBS 利用核变点检测来划分退化阶段，确保源域和目标域的小批量数据在对齐过程中按其失效阶段进行同步。作为补充，CAFLAE 结合了大核时间特征提取与交叉注意力机制，以学习更优的领域不变表示。该框架在韩国国防系统数据集和 XJTU-SY 轴承数据集上进行了严格验证，与现有最先进方法相比，平均性能提升了 24.1%。结果表明，DSSBS 通过阶段一致性采样改善了跨域对齐，而 CAFLAE 为长期工业状态监测提供了一个高性能的主干网络。

## Abstract
The construction of high quality health indicators (HIs) is crucial for effective prognostics and health management. Although deep learning has significantly advanced HI modeling, existing approaches often struggle with distribution mismatches resulting from varying operating conditions. Although domain adaptation is typically employed to mitigate these shifts, two critical challenges remain: (1) the misalignment of degradation stages during random mini-batch sampling, resulting in misleading discrepancy losses, and (2) the structural limitations of small-kernel 1D-CNNs in capturing long-range temporal dependencies within complex vibration signals. To address these issues, we propose a domain-adaptive framework comprising degradation stage synchronized batch sampling (DSSBS) and the cross-domain aligned fusion large autoencoder (CAFLAE). DSSBS utilizes kernel change-point detection to segment degradation stages, ensuring that source and target mini-batches are synchronized by their failure phases during alignment. Complementing this, CAFLAE integrates large-kernel temporal feature extraction with cross-attention mechanisms to learn superior domain-invariant representations. The proposed framework was rigorously validated on a Korean defense system dataset and the XJTU-SY bearing dataset, achieving an average performance enhancement of 24.1% over state-of-the-art methods. These results demonstrate that DSSBS improves cross-domain alignment through stage-consistent sampling, whereas CAFLAE offers a high-performance backbone for long-term industrial condition monitoring.

---

## 论文详细总结（自动生成）

以下是对论文《Domain-Adaptive Health Indicator Learning with Degradation-Stage Synchronized Sampling and Cross-Domain Autoencoder》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在工业设备的预测与健康管理（PHM）中，构建高质量的**健康指标（Health Indicator, HI）**是评估设备退化程度的关键。然而，现有深度学习方法在实际应用中面临两个核心挑战：
*   **分布偏移与阶段失配**：不同工况（如转速、负载变化）导致数据分布不一致。传统的领域自适应（DA）方法在训练时采用随机小批量采样，会将处于不同退化阶段（如健康期与剧烈退化期）的源域和目标域样本混合，导致计算出的差异损失（如 MMD）产生误导，干扰对齐效果。
*   **感受野受限**：常用的 1D-CNN 往往使用小卷积核，难以捕获工业振动信号中长程的时间依赖关系，导致提取的退化特征不充分。

### 2. 论文提出的方法论
论文提出了一个结合采样策略优化与新型网络架构的领域自适应框架：
*   **退化阶段同步批量采样（DSSBS）**：
    *   利用**核变点检测（Kernel Change-Point, KCP）**算法，根据信号的均方根（RMS）特征将全寿命周期数据自动划分为不同的退化阶段。
    *   在训练过程中，确保每一个 mini-batch 内的源域和目标域样本都处于**相同的退化阶段**，从而实现阶段同步的对齐，避免误导性的梯度更新。
*   **跨域对齐融合大自编码器（CAFLAE）**：
    *   **并行现代多尺度时间卷积（PMTC）**：借鉴 ModernTCN 架构，采用大卷积核（如 13, 23, 31）和深度可分离卷积，显著扩大有效感受野（ERF），以捕获长程退化趋势。
    *   **跨域注意力机制（Cross-Attention）**：在编码器中引入交叉注意力，促进源域和目标域之间的信息交换，学习更鲁棒的领域不变表征。
    *   **动态权重平均（DWA）**：自动调整形状约束损失（SCF）、对齐损失（MMD）和重构损失（REC）的权重，无需人工调参。

### 3. 实验设计
*   **数据集**：
    1.  **韩国国防系统（KWS）数据集**：包含冷却系统（制冷压缩机和空气压缩机）在实际运行中的全寿命数据。
    2.  **XJTU-SY 轴承数据集**：经典的实验室加速退化数据集，包含三种工况下的轴承振动信号。
*   **Benchmark 与对比方法**：
    *   **同工况模型**：DCAE、SMSAE、TDCAE（验证基础性能）。
    *   **跨工况（DA）模型**：TQFMDCAE、HCPTSCAE（验证迁移能力）。
*   **评价指标**：单调性（Mon）、相关性（Cor）、鲁棒性（Rob）以及综合指标（CI）。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件型号（如 GPU 种类和数量）及总训练时长。
*   但在模型复杂度分析中提到：CAFLAE 的参数量约为 **0.42M**，推理时间为 **1.87ms**。作者强调该模型在参数量较少的情况下实现了高性能，适合工业实时监测。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同性质的数据集（国防实测与实验室仿真）上进行了验证，涵盖了多种跨工况迁移任务（如 1→2, 2→1, 1→3 等）。
*   **重复性**：所有实验均独立运行 **10 次**并取平均值，保证了结果的统计可靠性。
*   **消融实验**：针对 DSSBS 采样策略、卷积核大小、多尺度设计、交叉注意力机制以及变点检测算法的选择分别做了详尽的消融研究。
*   **可视化分析**：通过 t-SNE 聚类可视化和有效感受野（ERF）热力图，直观展示了模型在特征对齐和长程特征捕获上的优势。实验设计整体非常充分且客观。

### 6. 主要结论与发现
*   **性能提升**：CAFLAE 框架在综合指标（CI）上比现有最先进方法平均提升了 **24.1%**。
*   **采样重要性**：DSSBS 显著稳定了训练过程，降低了损失函数的波动（通过 PI-Control 指标验证），证明了阶段同步采样是解决跨域 HI 构建的关键。
*   **大核优势**：实验证实大卷积核比小卷积核更能有效提取反映设备长期退化趋势的特征。
*   **HI 质量**：生成的 HI 曲线在 [0, 1] 范围内表现出极佳的单调性和平滑度，能够更准确地指示失效点，避免了提前误报。

### 7. 优点
*   **创新采样视角**：首次明确指出随机采样在退化建模中的“阶段失配”问题，并给出了基于变点检测的解决方案。
*   **架构先进性**：将大核卷积（ModernTCN 思想）引入 PHM 领域，解决了 1D-CNN 感受野不足的顽疾。
*   **实用性强**：引入 DWA 自动平衡多任务损失，降低了模型部署时的调参门槛。

### 8. 不足与局限
*   **阶段划分依据单一**：目前 DSSBS 主要依赖 RMS 特征进行阶段划分，对于某些初期退化极其微弱或受高频噪声干扰严重的设备，仅靠 RMS 可能无法精准捕捉变点。
*   **短程特征可能丢失**：虽然大核卷积有利于捕获长程趋势，但在某些突发性故障（短时冲击）的捕捉上可能不如小核敏感。
*   **应用范围限制**：该方法依赖于源域有完整的全寿命周期（Run-to-Failure）标签，在完全缺乏失效历史的新设备上难以直接应用。

（完）
