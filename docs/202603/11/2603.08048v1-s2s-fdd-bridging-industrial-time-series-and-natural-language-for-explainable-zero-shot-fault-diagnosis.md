---
title: "S2S-FDD: Bridging Industrial Time Series and Natural Language for Explainable Zero-shot Fault Diagnosis"
title_zh: S2S-FDD：连接工业时间序列与自然语言，实现可解释的零样本故障诊断
authors: "Baoxue Li, Chunhui Zhao"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08048v1"
tags: ["query:rul-dt"]
score: 9.0
evidence: 工业系统可解释零样本故障诊断
tldr: 针对工业故障诊断中传统模型缺乏可解释性及大语言模型难以直接处理高维传感器信号的问题，本文提出S2S-FDD框架。该框架通过创新的信号语义算子将抽象的时间序列转化为自然语言描述，并结合多轮树状诊断方法和历史维护文档，实现了可解释的零样本故障诊断，同时支持人机交互反馈以持续优化诊断性能。
selection_source: fresh_fetch
motivation: 传统诊断模型输出过于抽象且大语言模型难以理解高维工业信号，导致无法回答故障原因及修复建议等关键问题。
method: 设计了信号语义算子将信号特征转化为文本摘要，并采用多轮树状诊断结构结合历史文档和动态信号查询进行推理。
result: 在多相流过程实验中，该方法成功验证了在零样本场景下实现可解释故障诊断的可行性与有效性。
conclusion: S2S-FDD框架有效桥接了工业信号与自然语言语义，为构建具备推理能力和高透明度的工业智能诊断系统提供了新方案。
---

## 摘要
故障诊断对于工业系统的安全运行至关重要。传统的诊断模型通常产生抽象的输出，如异常评分或故障类别，无法回答诸如“为什么”或“如何修复”等关键运维问题。虽然大语言模型（LLMs）具有强大的泛化和推理能力，但它们在离散文本语料库上的训练使得在处理高维、时序工业信号时存在语义鸿沟。为了应对这一挑战，我们提出了一个信号到语义（Signals-to-Semantics）故障诊断（S2S-FDD）框架，通过两项关键创新将高维传感器信号与自然语言语义相连接：我们首先设计了一个信号到语义算子，将抽象的时间序列信号转换为自然语言摘要，捕捉趋势、周期性和偏差。基于这些描述，我们设计了一种多轮树状结构诊断方法，通过参考历史维护文档并动态查询额外信号来进行故障诊断。该框架进一步支持人机回环反馈，以实现持续优化。在多相流过程上的实验证明了所提方法在可解释零样本故障诊断方面的可行性和有效性。

## Abstract
Fault diagnosis is critical for the safe operation of industrial systems. Conventional diagnosis models typically produce abstract outputs such as anomaly scores or fault categories, failing to answer critical operational questions like "Why" or "How to repair". While large language models (LLMs) offer strong generalization and reasoning abilities, their training on discrete textual corpora creates a semantic gap when processing high-dimensional, temporal industrial signals. To address this challenge, we propose a Signals-to-Semantics fault diagnosis (S2S-FDD) framework that bridges high-dimensional sensor signals with natural language semantics through two key innovations: We first design a Signal-to-Semantic operator to convert abstract time-series signals into natural language summaries, capturing trends, periodicity, and deviations. Based on the descriptions, we design a multi-turn tree-structured diagnosis method to perform fault diagnosis by referencing historical maintenance documents and dynamically querying additional signals. The framework further supports human-in-the-loop feedback for continuous refinement. Experiments on the multiphase flow process show the feasibility and effectiveness of the proposed method for explainable zero-shot fault diagnosis.

---

## 论文详细总结（自动生成）

以下是对论文《S2S-FDD: Bridging Industrial Time Series and Natural Language for Explainable Zero-shot Fault Diagnosis》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的工业故障诊断（FDD）模型（如深度学习或统计方法）输出过于抽象（仅提供异常评分或类别标签），无法解释“为什么发生故障”以及“如何修复”。
*   **研究动机**：
    *   **语义鸿沟**：大语言模型（LLM）虽具备强推理能力，但难以直接理解高维、连续、动态的工业传感器信号。
    *   **数据稀缺**：工业环境下故障样本极少，传统监督学习难以覆盖所有未知故障。
    *   **可解释性需求**：工业安全运维需要透明的决策过程和专家可理解的自然语言建议。

### 2. 方法论
该论文提出了 **S2S-FDD（信号到语义故障诊断）** 框架，核心包含两个关键组件：

*   **信号到语义（S2S）算子**：
    *   **重构逻辑**：利用正常工况数据通过 K-means 聚类构建状态矩阵 $D$。对于输入信号，通过最小二乘法计算权重向量 $\omega$，将其重构为正常模式的线性组合。
    *   **异常识别**：计算原始信号与重构信号的残差（RES）。若残差超过基于基准误差的阈值，则判定为异常。
    *   **文本转换**：将受影响传感器的趋势（上升/下降）、周期性、偏差百分比等数值特征，根据预设模板转化为自然语言摘要（如“传感器 PT312 在 150s 后下降了 20%”）。
*   **多轮树状结构诊断方法**：
    *   **知识检索**：将生成的文本描述与历史维护记录（知识库）进行向量匹配，检索相关故障案例。
    *   **动态推理**：LLM 充当推理引擎。如果初始信息不足，LLM 可以通过“函数调用（Function Calling）”动态查询其他传感器的详细数据。
    *   **人机回环**：支持专家对推理过程进行反馈和修正，并将验证后的诊断报告存入知识库，实现知识迭代。
*   **理论支撑**：论文提供了残差能量边界定理，证明了该重构模块在理论上能够区分健康状态与故障状态。

### 3. 实验设计
*   **实验场景**：英国克兰菲尔德大学（Cranfield University）的多相流过程（Multiphase Flow Process）设施，涉及水、油、气混合输送系统。
*   **数据集**：选取了 5 种典型故障类型（气路堵塞、水路堵塞、顶部分离器堵塞、段塞流、2英寸管线加压），共 13 个测试案例。
*   **对比方法（Benchmark）**：
    *   **非推理模型**：Qwen2.5 (7B/72B), DeepSeek-V3。
    *   **推理模型（Reasoning LLMs）**：DeepSeek-R1-Distill (7B/32B), QwQ-32B, DeepSeek-R1 (Full)。
*   **评估指标**：ACC@5（每个案例推理 5 次并投票，计算最终准确率）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练/推理时长。由于该框架主要基于现有的预训练 LLM 进行 Prompt Engineering 和零样本推理，不涉及大规模模型重训练，因此对本地算力的依赖主要取决于所调用的模型 API 或本地部署环境。

### 5. 实验数量与充分性
*   **实验规模**：针对 13 个具体故障案例进行了测试，每个案例重复 5 次以确保统计稳定性。
*   **充分性评价**：
    *   **模型覆盖广**：对比了从 7B 到超大规模参数、从通用型到推理型的多种主流 LLM，实验结果具有代表性。
    *   **场景针对性强**：多相流过程是工业诊断领域的经典基准。
    *   **局限性**：实验案例总数（13个）相对较少，且仅在一个工业场景上进行了验证，对于更复杂、更大规模的跨行业工业系统的泛化性仍有待进一步验证。

### 6. 主要结论与发现
*   **零样本可行性**：在完全不使用故障样本训练的情况下，DeepSeek-R1 达到了 **76.92%** 的诊断准确率，证明了“信号转语义”路径的有效性。
*   **推理模型优势**：带有思维链（CoT）推理能力的模型（如 DeepSeek-R1）显著优于普通指令微调模型（如 Qwen2.5-7B 仅 23.08%）。
*   **参数规模正相关**：在推理模型中，参数量越大（如从 7B 到 32B 再到 Full 版），诊断的逻辑严密性和准确性越高。
*   **可解释性提升**：模型不仅给出了故障编号，还提供了排除其他故障的逻辑依据（如 FT305 流量上升与气路堵塞矛盾）。

### 7. 优点与亮点
*   **创新性桥接**：通过 S2S 算子解决了工业时序数据与文本语义不匹配的底层难题。
*   **零样本诊断**：极大降低了工业场景下对标注故障数据的依赖。
*   **动态交互**：树状诊断结构允许模型像人类专家一样“按需查询”数据，而非一次性输入所有高维数据，降低了 LLM 的上下文压力。
*   **高透明度**：输出包含完整的推理链条，符合工业领域对安全决策的合规性要求。

### 8. 不足与局限
*   **实时性挑战**：多轮对话和 LLM 的推理延迟可能限制其在毫秒级实时监控场景中的应用。
*   **Prompt 敏感性**：诊断结果高度依赖于 [PROCESS_INFO] 等背景知识的描述质量，Prompt 的微小变动可能影响结果。
*   **复杂故障识别**：对于多个故障并发或极其微弱的早期故障，仅靠趋势描述可能不足以支撑精准判断。
*   **验证范围**：目前仅在单一实验装置上验证，缺乏在实际化工厂、电网等超大规模系统中的实测数据。

（完）
