---
title: "LoRM: Learning the Language of Rotating Machinery for Self-Supervised Condition Monitoring"
title_zh: LoRM：学习旋转机械语言以实现自监督状态监测
authors: "Xiao Qin, Xingyi Song, Tong Liu, Hatim Laalej, Zepeng Liu, Yunpeng Zhu, Ligang He"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05863v1"
tags: ["query:rul"]
score: 9.5
evidence: 旋转机械状态监测的自监督框架
tldr: LoRM是一个自监督框架，将旋转机械信号视为“机器语言”，通过信号Token化和预训练语言模型微调实现实时状态监测。该方法无需手工特征，通过预测误差衡量健康状况。实验表明其在刀具监测中具有稳定的实时跟踪和强泛化能力，为工业信号分析提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05863v1/fig-001.webp\", \"caption\": \"Fig. 1: Training through token prediction: from ASR, LMs, and VLMs to LoRM. LoRM extends this paradigm to rotating machinery by predicting discrete future signal tokens from observed multi-sensor context.\", \"page\": 2, \"index\": 1, \"width\": 516, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2604.05863v1/fig-002.webp\", \"caption\": \"Fig. 3: Overview of the TCM case study. (a) Front view of the DMU 40 eVo CNC milling machine and the deployed sensing system, including a power sensor, tri-axis spindle accelerometer, a tri-axis plate accelerometer, a tri-axis force sensor and a microphone. (b) Examples of the collected multi-modal sensor signals. (c) Illustration of the dynamic milling strategy. (d) Evolution of the maximum tool wear measured from the four-flute cutters for Tool 1 to 3 using a microscope.\", \"page\": 7, \"index\": 2, \"width\": 968, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2604.05863v1/fig-003.webp\", \"caption\": \"Table I: Analogy between language-modelling concepts and LoRM.\", \"page\": 3, \"index\": 3, \"width\": 524, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2604.05863v1/fig-004.webp\", \"caption\": \"Table III: Cross-Tool Prediction Performance of Different Methods\", \"page\": 8, \"index\": 4, \"width\": 1076, \"height\": 725}, {\"url\": \"assets/figures/arxiv/2604.05863v1/fig-005.webp\", \"caption\": \"Fig. 2: Overall architecture and learning mechanism of LoRM. (a) A multi-channel signal window is divided into a context segment and a target segment, and the context segment is partitioned and flattened into a multi-sensor patch sequence. (b) The patch sequence is projected into the hidden space of a pre-trained language model with positional embeddings. (c) A partially fine-tuned pre-trained language model encodes temporal and cross-channel dependencies. (d) The contextualised patch features are mean-pooled into a global representation and transformed into channel-wise token probability distributions. (e) The target segment of each channel is independently tokenised by k-means clustering to generate ground-truth tokens. (f) A cross-entropy objective is applied between the predicted distributions and the generated tokens for self-supervised training.\", \"page\": 4, \"index\": 5, \"width\": 968, \"height\": 427}]"
motivation: 传统信号处理依赖手工特征且难以处理复杂多模态数据，需要一种更通用、自监督的方式来理解旋转机械信号。
method: 将多模态传感器数据重构为Token序列预测问题，通过量化未来信号并微调预训练语言模型来捕捉信号演化规律。
result: 在实地刀具状态监测实验中，LoRM实现了稳定的实时状态跟踪，并展现出优异的跨工具泛化能力。
conclusion: LoRM成功将语言建模技术应用于工业信号分析，证明了基于Token的序列预测在旋转机械状态监测中的实用性和有效性。
---

## 摘要
我们提出了 LoRM（Language of Rotating Machinery），这是一个用于多模态旋转机械信号理解和实时状态监测的自监督框架。LoRM 构建于旋转机械信号可被视为一种机器语言的理念之上：局部信号可以被标记化为离散的符号单元，并且可以根据观察到的多传感器上下文预测其未来的演变。与依赖手工设计变换和特征的传统信号处理方法不同，LoRM 将多模态传感器数据重新表述为基于标记的序列预测问题。对于每个数据窗口，观察到的上下文片段以连续形式保留，而每个传感通道的未来目标片段则被量化为离散标记。随后，通过在工业信号上对通用预训练语言模型进行部分微调，实现了高效的知识迁移，从而避免了从头训练大模型的需要。最后，通过将标记预测误差作为健康指标进行跟踪来执行状态监测，误差的增加即代表性能退化。原位刀具状态监测（TCM）实验证明了稳定的实时跟踪能力和强大的跨刀具泛化性，表明 LoRM 在语言建模与工业信号分析之间架起了一座实用的桥梁。源代码已在 https://github.com/Q159753258/LormPHM 公开。

## Abstract
We present LoRM (Language of Rotating Machinery), a self-supervised framework for multi-modal rotating-machinery signal understanding and real-time condition monitoring. LoRM is built on the idea that rotating-machinery signals can be viewed as a machine language: local signals can be tokenised into discrete symbolic units, and their future evolution can be predicted from observed multi-sensor context. Unlike conventional signal-processing methods that rely on hand-crafted transforms and features, LoRM reformulates multi-modal sensor data as a token-based sequence-prediction problem. For each data window, the observed context segment is retained in continuous form, while the future target segment of each sensing channel is quantised into a discrete token. Then, efficient knowledge transfer is achieved by partially fine-tuning a general-purpose pre-trained language model on industrial signals, avoiding the need to train a large model from scratch. Finally, condition monitoring is performed by tracking token-prediction errors as a health indicator, where increasing errors indicate degradation. In-situ tool condition monitoring (TCM) experiments demonstrate stable real-time tracking and strong cross-tool generalisation, showing that LoRM provides a practical bridge between language modelling and industrial signal analysis. The source code is publicly available at https://github.com/Q159753258/LormPHM.

---

## 论文详细总结（自动生成）

### 论文总结：LoRM —— 学习旋转机械语言以实现自监督状态监测

#### 1. 核心问题与整体含义
旋转机械（如电机、涡轮、机床）在工业中至关重要，其故障会导致巨大经济损失。传统的状态监测方法高度依赖专家知识和手工设计的特征提取，且难以有效融合来自振动、声学、力等不同传感器的异构多模态数据。
**LoRM 的核心动机**是将旋转机械信号视为一种“机器语言”。论文认为信号中存在重复的局部模式（类似单词），且其演化受物理动力学约束（类似语法）。通过将信号理解转化为“Token（标记）预测”问题，可以利用预训练语言模型（PLM）的强大序列建模能力，实现无需人工标注的自监督状态监测。

#### 2. 方法论核心思想与技术细节
LoRM 框架将多传感器信号处理流程重构为类似自然语言处理的架构：
*   **上下文-目标划分（Context-Target Formulation）**：将每个信号窗口分为“上下文段”（连续波形，作为输入）和“目标段”（紧随其后的信号，用于生成预测标签）。
*   **信号标记化（Tokenization）**：对每个传感通道独立使用 **K-means 聚类**，将连续的目标段波形映射为离散的符号（Token）。这降低了对噪声和振幅波动的敏感性。
*   **多传感器融合（Code-switching 启发）**：借鉴多语言建模中的语码转换，将不同通道的补丁（Patches）按预定义顺序展平为一个统一序列，使 Transformer 能够同时建模时间和跨通道的依赖关系。
*   **模型架构与微调**：
    *   使用线性嵌入层将 Patch 投影到隐藏空间。
    *   采用**部分微调策略**：冻结预训练语言模型（如 GPT-2）的大部分参数，仅训练嵌入层、位置编码、归一化层和预测头，提高训练效率并保留通用序列知识。
*   **健康指标（HI）计算**：通过跟踪模型预测未来 Token 的误差（交叉熵损失）来衡量机器状态。当机器退化时，信号偏离正常模式，预测误差随之增大，从而触发预警。

#### 3. 实验设计
*   **场景与数据集**：在英国谢菲尔德大学 AMRC 进行的原位刀具状态监测（TCM）实验。使用 5 轴 CNC 铣床对钛合金进行切削，记录了全寿命周期的刀具磨损数据。
*   **传感器配置**：包含 11 个通道（3 轴主轴加速度、3 轴工作台加速度、3 轴切削力、主轴功率、麦克风声学信号）。
*   **Benchmark 与对比方法**：
    *   **传统统计方法**：基于马氏距离的统计特征方法。
    *   **不同 PLM 后干**：GPT-2（预训练 vs 从头训练）、BERT、T5、语音预训练模型 Whisper。
    *   **时间序列模型**：GPT4TS（基于连续回归的方法）。
*   **验证方式**：跨工具泛化测试（在刀具 T1 上训练，在 T2、T3 上测试），这是衡量工业模型实用性的关键指标。

#### 4. 资源与算力
*   **硬件环境**：Ubuntu 系统，24 GB 内存，**NVIDIA GeForce RTX 4090 GPU**。
*   **软件实现**：Python 3.10.12，代码已开源。
*   **训练细节**：文中未明确给出具体的训练时长，但提到由于采用了部分微调（Partial Fine-tuning）策略，训练效率显著优于从头训练大模型。

#### 5. 实验数量与充分性
论文进行了较为充分的实验验证：
*   **跨工具验证**：分别以 T1、T2、T3 作为开发工具进行三组完整的交叉验证。
*   **后干模型对比**：对比了 5 种不同的 Transformer 架构及其预训练状态。
*   **消融实验**：对比了“离散 Token 预测”与“连续波形回归”的优劣。
*   **评价指标**：使用了准确率、精确率、召回率、F1 分数、误报率（FPR）以及绝对磨损预测误差。
**评价**：实验设计考虑了工业场景中的类不平衡问题（正常样本多于异常样本），并重点测试了跨工况的泛化性，实验结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **Token 化的优势**：将波形预测转化为离散分类任务比直接进行连续回归（如 GPT4TS）更稳定，能显著降低跨工具测试中的误差。
*   **预训练的重要性**：使用在大规模文本或语音上预训练的模型作为初始化，比从头训练模型具有更强的鲁棒性和泛化能力。
*   **架构敏感性**：自回归预训练模型（如 GPT-2）在旋转机械信号预测任务中表现优于掩码语言模型（如 BERT）。
*   **实时监测能力**：LoRM 能够生成平滑的健康指数曲线，准确捕捉刀具磨损趋势，且误报率极低。

#### 7. 优点
*   **新颖的视角**：首次系统性地提出了“旋转机械语言”概念，为工业信号处理提供了统一的语言建模视角。
*   **自监督学习**：无需密集的故障标签，仅需正常运行数据即可训练，极具工业应用前景。
*   **强大的融合能力**：通过 Patch 展平技术，巧妙解决了多模态、高频率工业信号的融合难题。
*   **跨工具泛化**：在实际工业现场数据上证明了模型在不同设备/刀具间的迁移能力。

#### 8. 不足与局限
*   **阈值依赖性**：在线监测仍需要根据初始缓冲区设定报警阈值 $\tau$，阈值的灵敏度可能影响预警效果。
*   **计算开销**：虽然是部分微调，但推理时仍需运行 Transformer 后干，对于极低功耗的嵌入式边缘设备可能仍有压力。
*   **场景覆盖**：目前仅在铣削加工（TCM）场景下验证，尚未在轴承、齿轮箱等其他旋转机械组件上进行大规模验证。
*   **Token 粒度**：K-means 聚类的簇数 $K$ 需要经验设定，不同 $K$ 值对复杂模式的捕捉能力可能存在偏差。

（完）
