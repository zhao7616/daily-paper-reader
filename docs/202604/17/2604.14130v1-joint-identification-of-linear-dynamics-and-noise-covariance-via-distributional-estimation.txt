Title: Joint Identification of Linear Dynamics and Noise Covariance via Distributional Estimation

URL Source: https://arxiv.org/pdf/2604.14130v1

Published Time: Thu, 16 Apr 2026 01:20:17 GMT

Number of Pages: 25

Markdown Content:
# Joint Identification of Linear Dynamics and Noise Covariance via Distributional Estimation 

Yang Hu a, ∗, Na Li a

> aHarvard University, 150 Western Ave, Boston, 02134, MA, USA

Abstract 

In this paper, we propose a novel framework for the joint identification of system dynamics and noise covariance in linear systems, under general noise distributions beyond Gaussian. Specifically, we would like to simultaneously estimate the dynamical matrix A and the noise covariance matrix Σ using state transition data. The formulation builds upon a novel parameterization of the state-transition distribution, which enables more e ff ective use of distributional “shape” information for improved identification accuracy. We introduce two practical estimators, namely the maximum likelihood estimator (MLE) and the score-matching estimator (SME), to solve the joint dynamics-covariance identification problem, and provide rigorous analysis of their statistical properties and sample complexity. Simulation results show that the proposed estimators outperform the ordinary least squares (OLS) baseline. 

Keywords: linear systems, system identification, noise covariance identification, maximum likelihood estimator, score matching, sample complexity 

1. Introduction 

System identification—the problem of estimating system dynamics using state transition data—has been a long-standing research field with broad applications, ranging from control and robotics to econometrics and neuroscience. In the past, classical theoretical results in this field mainly focused on the consistency (i.e., asymptotic convergence) of estimators, including ordinary least-squares (OLS) estimators [ 1] and maximum likelihood estimators (MLE) [ 2 ]. The more recent advances in learning theory have brought significant progresses in establishing finite-time convergence guarantees for these estimators [ 3, 4 , 5 , 6]. These developments have, in turn, laid foundations for finite-time regret analyses in downstream control synthesis tasks [7, 8, 9, 10]. However, these finite-time analyses of linear system identification have primarily focused on estimating the nominal system dynamics (e.g., the dynamic matrix A for autonomous linear systems), typically under strong simplifying assumptions on the noise model like isotropic Gaussian or sub-Gaussian noise. Such assumptions do facilitate tractable analysis and favorable sample complexity guarantees, though at the  

> ∗Yang Hu (email: yanghu@g.harvard.edu ) is the corresponding author of this paper.
> arXiv:2604.14130v1 [eess.SY] 15 Apr 2026

risk of over-simplifying real-world systems by ignoring the crucial “shape” information hidden in the noise distribution, such as its tail behavior and higher-order moments. For instance, the success of OLS-based estimators in identifying nominal dynamics is known to implicitly rely on the light-tailed nature of the noise, which may not hold in practice and could lead to performance degradation. Beyond the question of whether such noise assumptions are realistic or not, another important yet often overlooked issue is the identification of the noise covariance structure itself. Even under the classical assumption of light-tailed Gaussian noise, estimating the noise covariance remains a meaningful and practically relevant task. Such information plays a pivotal role in a variety of downstream applications, including distributionally robust control [ 11 , 12 ], risk-sensitive control [ 13 , 14 ], and adaptive Kalman filtering [ 15 ,16 ], where knowledge of the noise covariance is typically assumed a priori. Therefore, there is a clear need to move beyond merely identifying the nominal dynamics and to develop e ffi cient methods for jointly identifying both the system dynamics and the underlying noise structure. These observations have recently spurred growing interest in noise modeling and identification. Following the pioneering work [ 17 ], multiple covariance identification methods have been designed in the past decade, including auto-covariance least-squares (ALS) methods [ 18 , 19 ] and MLE methods [ 20 , 21 ]. However, these methods only focus on the noise covariance identification and generally assume known system dynamics. In brief, while established methods exist for estimating system dynamics or noise covariance separately, principled approaches for the joint identification of both are still lacking. One intuitive approach to bridge the above gap between system identification and noise covariance identification is to resort to a two-stage scheme—first identifying the nominal dynamics, and then recovering the noise covariance using the identified dynam-ics. However, as discussed later in this paper, such a tandem approach is technically equivalent to assuming Gaussian noise, thereby failing to capture the richer “shape” information presented in more general noise models. These challenges motivate the following research question: 

Is it possible to perform joint identification of dynamics and noise covariance for generic noise distribution? 

Here by “joint identification” we explicitly mean to rule out the tandem approach stated above. Instead, we would like to reparameterize the problem to encapsulate both the dynamics and the covariance into a uniform set of parameters, hence enabling algorithms that e ff ectively and e ffi ciently estimate the parameters for the joint identification. As we will see later, an appropriate generic parameterization scheme is critical to this task. 

Contributions. In this paper, we propose to model noise distribution as a unit-covariance base distribution scaled by the covariance matrix, such that the state transition distri-bution belongs to a density family parameterized by the dynamical matrix A and the scale matrix Σ. Such reparameterization is favored in that it is general enough to include a wide range of known noise distributions, and that it directly encodes A and Σ in the parameters to reformulate the joint identification task as a parameter estimation problem. Based on such reparameterization, we propose two novel estimators, i.e. the maximum likelihood estimator (MLE) and the score-matching estimator (SME), that outperform 2the naive ordinary least-squares (OLS) estimator via theoretical arguments and empirical evidence. The contribution of this paper is two-fold. 1. We establish the connection between state transition in linear systems subject to generic stochastic noise and ϕ-density families, and highlight an important sub-collection of elliptical families that greatly facilitates computation. As far as we know, this is the first paper to study the problem of joint identification of linear dynamics and noise covariance (the “ joint identification ” problem) and propose an appropriate model to reformulate it as a parameter estimation problem. 2. We propose two novel estimators, namely MLE and SME, to solve the joint identifica-tion problem, along with a characterization of their key properties and a preliminary sample complexity analysis. The performance of the estimators is compared against the naive OLS baseline and justified by empirical evaluation results. The rest of this paper is organized as follows. In Section 2 we introduce the system setting and problem formulation, as well as preliminaries on ϕ-density families. In Section 3 we define the proposed estimators and characterize their theoretical properties, and also provide a sample complexity analysis. In Section 4 we include simulation results that further justify the performance of the proposed estimators. 

2. Problem Formulation and Preliminaries 

In this section, we first formulate our system setting and the joint identification problem, and then provide some necessary preliminaries on the candidate distribution families. 

2.1. Problem Formulation 

System Setting. Consider a linear time-invariant (LTI) system 

x′ = A⋆ x + Σ1/2 

> ⋆

w, (1) where x, x′ ∈ Rd denote the current and next state of the system, respectively; A⋆ ∈ Rd×d

is the dynamical matrix, and Σ⋆ ∈ S d 

> ≻0

is the scale matrix that regulates the covariance of the e ff ective noise input Σ1/2 

> ⋆

w, both of which are unknown and to be estimated; w ∈ Rd

is an i.i.d. unit noise with zero mean ( E[w] = 0) and unit covariance ( Var[ w] = I). We emphasize that, as discussed in the introduction, we would like to handle general noise distributions, rather than being confined to any specific choices like Gaussian noise. Specifically, we assume a known base density ϕ(·) of the unit noise w, which is allowed to take any form, as long as it satisfies the following regularity assumptions. 

Assumption 1 (Base density) . The base density ϕ is positive and twice continuously di ff erentiable, and induces a finite first-order moment. Further, denote the gradient and Hessian of log ϕ(w) by g(w) and H(w), respectively; i.e., 

g(w) : = ∇w log ϕ(w) = ∇wϕ(w)

ϕ(w) ,

H(w) : = ∇2 

> w

log ϕ(w) = ∇wg(w).

3As will be seen later, the parameter estimators generally involve log-densities and their derivatives, and the assumption is hence made to facilitate the design of such estimators. 

The Joint Identification Problem. On a high level, the goal of the proposed joint identification problem is to identify the dynamical and scale matrices ( A, Σ ), using state transition data D = {(xi, x′ 

> i

) | i ∈ [N]}. Note that, in general, we don’t make assumptions on the dataset D, which is allowed to contain both single- and multiple-trajectory data. A natural and concrete way to solve this joint identification problem is to convert it into a parameter estimation problem, which leads to various well-known estimators including the maximum likelihood estimator (MLE) and the score-matching estimator (SME) proposed below. Specifically, for any loss function ℓ(x, x′; A, Σ ) penalizing “less likely” transitions from x to x′ under parameters ( A, Σ ), the corresponding estimator is ( ˆ A, ˆΣ) : = arg min 

> A,Σ
> N

X

> i=1

ℓ(xi, x′ 

> i

; A, Σ ). (2) The high-level goal of the remaining paper is to construct proper losses ℓ and charac-terize the corresponding estimators in terms of statistical properties including sample complexity. 

2.2. Preliminaries 

In this section, we introduce some preliminaries on how to properly reparameterize the noise distribution. We first show that the transition distribution implied by (1) belongs to a family determined by the base density ϕ, and then introduce elliptical 

family as a computation-friendly special case. For the sake of presentation, we only focus on distributions with densities. 

Reparameterizing as ϕ-Density Family. We point out again that various methods have been proposed in literature to perform either dynamics identification (estimate A

given known Σ⋆) [ 3, 4, 5, 6] or covariance identification (estimate Σ given known A⋆)[ 18 , 19 , 20 , 21 ] separately using state transition data, but not for joint identification 

that simultaneously identifies ( A, Σ ) (both assumed to be unknown). A major issue that obstructs the application of data-driven methods for joint identification is that the roles played by the A and Σ matrices in state transitions are quite distinct. Indeed, conditioned on any fixed current state x, the dynamics (1) guarantees that the conditional expectation and variance of the next state x′ are, respectively, 

E[x′ | x] = E[A⋆ x | x] + E[Σ1/2 

> ⋆

w | x] = A⋆ x,

Var[ x′ | x] = Var[ A⋆ x | x] + Var[ Σ1/2 

> ⋆

w | x] = Σ⋆.

Briefly put, the expectation only depends on A⋆, while the variance is fully determined by Σ⋆, which indicates that they have to be handled independently without a unified reparameterization of the problem that treats the parameters ( A, Σ ) equally. Fortunately, the introduction of a fixed base density ϕ(·) provides su ffi cient additional structure that enables such reparameterization. Note that, as dictated by the linear dynamics (1) , given a known base density ϕ(w) of the unit-covariance noise w, the state transition distribution P(x′|x) must also be associated with a conditional density 

fA,Σ (x′|x) given by 

fA,Σ (x′|x) = det( Σ)−1/2ϕΣ−1/2(x′ − Ax ), (3) 4which is parameterized by, and only by, ( A, Σ ). We say that P(x′|x) belongs to a ϕ-density family 1. We will denote the the gradient and Hessian of log ϕ(w) as g(w) and 

H(w), respectively, 

g(w) = ∇w log ϕ(w); H(w) = ∇2 

> w

log ϕ(w) (4) For technical clarity, we further assume that the hypothesis space Θ for ( A, Σ )satisfies the following regularity assumption. 

Assumption 2 (Hypothesis space) . Let Θ = ΘA × ΘΣ be the product of hypothesis spaces for A and Σ, where ΘA ⊆ Rd×d and ΘΣ ⊆ S d

> ≻0

. We assume that θ⋆ = (A⋆, Σ ⋆)lies in the interior of Θ, and that fA1,Σ 1 (x′|x) ≡ fA2,Σ 2 (x′|x), ∀x, x′ ∈ Rd if and only if (A1, Σ 1) = (A2, Σ 2) ∈ Θ.The benefit of fixing a base density and considering the corresponding ϕ-density family is that it provides a unified parameterization of transition distributions using (A, Σ ) (rather than the distribution-specific parameters), directly exposing the parameters to learn and thus simplifying the design of estimators. 

The Elliptical Family. Despite the generality of ϕ-density families, it could be ana-lytically and computationally hard to handle the log-densities and their derivatives in closed forms. To further facilitate the design and analysis of algorithms, we consider an important special type of ϕ-density families, where their base densities are assumed to be elliptical (i.e., the iso-density contours are ellipsoids; see [23] for details). 

Definition 1 (Elliptical density) . A density ϕ is called elliptical if there exists a function 

ψ : R → R such that ϕ(w) = ψ(w⊤w). Elliptical base densities are favorable since the derivatives of their log-densities can be written in simple closed forms. 

Lemma 1. For an elliptical base density ϕ(w) = ψ(w⊤w), the gradient g(w) and Hessian H(w) of log ϕ(w) are given by 

g(w) = ρ(w⊤w)w, (5a) 

H(w) = ρ(w⊤w)I + τ(w⊤w)ww ⊤, (5b) 

where the functions ρ : R → R and τ : R → R are specified by 

ρ(z) : = 2 ddz

log ψ(z) = 2ψ′(z)

ψ(z) ,τ(z) : = 4 d2

dz2

log ψ(z) = 4 ψ′′ (z)ψ(z) − ψ′(z)2

ψ2(z) .

Proof. By direct calculation. See Appendix A for details. We would like to highlight that a wide range of named distributions fall into the category of elliptical distributions. As a sanity check, we first show that the Gaussian family is elliptical.      

> 1This is closely related to the location-scale family in probability theory literature, which refers to a collection of distributions /densities parameterized by location and scale parameters. In our case, Ax and Σ
> can be viewed as the location and the scale, respectively. An expository discussion can be found in [22].

5Example 1 (Multivariate Gaussian family) . The log-density of the unit d-dimensional Gaussian distribution N(0, Id) is log ϕN (w) = − 12 w⊤w − d

2 log(2 π),

which is a function of w⊤w and is hence elliptical. It can be verified that the derivatives of log ϕN (w) are specified by (5), with ρN and τN given by 

ρN (z) = −1, τN (z) = 0.

We proceed to introduce another elliptical density that will be extensively used in simulations. 

Example 2 (Multivariate Student-t family) . The log-density of the d-dimensional unit Student-t distribution td,ν 

 0, ν−2 

> ν

Id

 with degree-of-freedom parameter ν > 2 is given by log ϕt(w) = − ν + d

2 log 1 + 1

ν − 2 w⊤w

!

,

which is a function of w⊤w and is hence elliptical. It can be verified that the derivatives of log ϕt(w) are specified by (5), with ρt and τt given by 

ρt(z) = − ν + d

(ν − 2) + z , τt(z) = 2( ν + d)(( ν − 2) + z)2 .

3. Joint Identification 

In this section, we provide our solutions to the joint identification problem for-mulated above. We start by a motivating section that illustrates the issue with naive ordinary least-squares (OLS) estimators. Then we proceed to introduce two well-known estimators that solve the problem, namely the maximum likelihood estimator (MLE) and the score-matching estimator (SME), along with their characterizations and convergence properties in our setting. Finally, we present a sample complexity guarantee for the proposed estimators. 

3.1. The Naive OLS estimator and its limitations 

At first glance, one may naturally wonder how this problem could potentially be di ff erent from the standard system identification problem that only identifies the A

matrix (see, e.g., [ 4]). Indeed, it is intuitive to come up with the following estimator ( ˆ Aols , ˆΣols ) that first identifies A by solving a least squares problem, i.e., ˆAols = arg min 

> AN

X

> i=1

∥x′ 

> i

− Ax i∥22, (6) and then estimates Σ by the sample covariance calculated using ˆAols . In closed-form formula, the naive OLS estimator is 

ˆAols = P Ni=1 x′ 

> i

x⊤

> i

P Ni=1 xi x⊤

> i

−1

,

ˆΣols = 1

> N

PNi=1

 x′ 

> i

− ˆAols xi

  x′ 

> i

− ˆAols xi

⊤. (7) However, a major concern of the above two-step procedure is that it completely ignores the information encoded in the ϕ-density family of candidate distributions. To briefly 6Estimator Formula Information                         

> OLS ˆAols :=arg min
> A
> PNi=1∥x′
> i−Ax i∥22
> ˆΣols :=1
> N
> PNi=1
>  x′
> i−ˆAols xi
>   x′
> i−ˆAols xi
> ⊤D
> MLE ( ˆ Amle ,ˆΣmle ) : =arg max
> A,Σ
> ˆLmle (A, Σ )D, log ϕ(·)SME ( ˆ Asme ,ˆΣsme ) : =arg min
> A,Σ
> ˆLsme (A, Σ )D,g(·), H(·)Table 1: An overview of OLS, MLE and SME estimators.

illustrate this point, we consider the 1-dimensional special case 2 where the transition dataset D = {(xi, x′ 

> i

)} is collected with current state fixed to xi ≡ 1, such that the naive OLS estimators become ( ˆAols = 1

> N

PNi=1 x′ 

> i

,

ˆΣols = 1

> N

PNi=1

 x′ 

> i

− ˆAols 

2. (8) In other words, in this case ( A, Σ ) coincides with the location-scale parameter of the distribution of x′, while its naive OLS estimator becomes the sample mean and variance. However, it is known that, in the identification problem of the best location parameter with any given scale parameter, the maximum likelihood estimator (MLE) coincides with the sample mean if and only if the candidate family is Gaussian [ 24 , 25 ], which agrees with a similar result established later of this paper, where MLE only coincides with the OLS estimator when the base density is Gaussian (see Lemma 9). This result implies that the naive OLS estimator given in (8) is not necessarily the most probable guess we can make given the candidate family of distributions. An intuitive reason behind such discrepancy is that the OLS formulation fails to take into consideration the “shape” of the distribution for identification, and thus its performance may deteriorate when the family is vastly di ff erent from Gaussian. We emphasize that such observation motivates the introduction of alternative estimators that can better leverage the “shape” information of distributions encapsulated in the candidate family, as shown in the following sections. 

The Proposed Estimators. For a brief overlook, we summarize the proposed OLS, MLE and SME estimators in Table 1 above, where the definition of losses ˆLmle and ˆLsme can be found in subsequent sections. We also include the information needed to calculate these estimators—OLS only requires the transition dataset, while MLE and SME require additional information like log-densities or their derivatives. In the following sections, we will proceed to introduce the proposed MLE and SME estimators in details. 

3.2. The Maximum Likelihood Estimator (MLE) 

We first define the maximum likelihood estimator (MLE) with respect to a ϕ-density family { fA,Σ } with base density ϕ(w).     

> 2Note that both Aand Σare scalars in this 1-dimensional case.

7(MLE) ( ˆ Amle , ˆΣmle ) : = arg max 

> A,Σ

ˆLmle (A, Σ ), (9) where ˆLmle (A, Σ ) : = 1

N

> N

X

> i=1

log fA,Σ (x′ 

> i

|xi)

= 1

N

> N

X

> i=1

hlog ϕΣ−1/2(x′ 

> i

− Ax i) + log det Σ−1/2i .

We point out that the empirical loss ˆLmle could be understood as the sample-based estimator of the expected loss 

Lmle (A, Σ ) : = E(x,x′)∼dD

log fA,Σ (x′|x).

Now we proceed to establish two fundamental properties of the MLE—(1) the unbiasedness of MLE, in the sense that the expected loss Lmle is uniquely maximized at the ground-truth parameters ( A⋆, Σ ⋆); and (2) the characterization of the MLE, which promotes theoretical understanding of the algorithm. 

Lemma 2 (consistency of MLE) . Under Assumption 2 and 3, (A⋆, Σ ⋆) is the unique maximizer of Lmle (A, Σ ).Proof. Note that, by Gibbs’ inequality we have 

Lmle (A, Σ ) =

ˆ

> x

dD(x)

ˆ

> x′

fA⋆,Σ ⋆ (x′|x) log fA,Σ (x′|x)d x′dx

≤

ˆ

> x

dD(x)

ˆ

> x′

fA⋆,Σ ⋆ (x′|x) log fA⋆,Σ ⋆ (x′|x)d x′dx,

where equality is achieved if and only if 

fA,Σ (·| x) = fA⋆,Σ ⋆ (·| x), ∀x ∈ Rd.

Then the proof is completed by applying Assumption 2. 

Lemma 3 (asymptotic normality of MLE) . Under Assumption 1 and 2, let ˆθ := ( ˆA, ˆΣ)

and θ⋆ := (A⋆, Σ ⋆), such that √N ˆθ−θ⋆

 d

→ N 0, 1

> I(θ)

, where I(θ) : = Var θ

∇θ log fθ(x)

denotes the Fisher information matrix. Proof. This is a direct corollary of the asymptotic normality results for general MLEs (see, e.g., Theorem 9.18 in [ 26 ]). In fact, it only su ffi ces to verify that fθ is di ff erentiable in θ (Assumption 1), θ⋆ lies in the interior of Θ (Assumption 2), and θ⋆ is the unique maximizer of Lmle (A, Σ ) (Lemma 2). 

Lemma 4 (MLE characterization) . Under Assumption 1, the MLE ( ˆAmle , ˆΣmle ) with respect to a ϕ-density family { fA,Σ } with base density ϕ satisfies the following system of equations: PNi=1 g ˆΣ−1/2mle 

 x′ 

> i

− ˆAmle xi

 x⊤ 

> i

= 0,

ˆΣ1/2mle + 1

> N

PNi=1 g ˆΣ−1/2mle 

 x′ 

> i

− ˆAmle xi

  x′ 

> i

− ˆAmle xi

⊤ = 0.

Recall that g(w) : = ∇w log ϕ(w) is the derivative of the log-base-density (see Assump-tion 1 for detailed definition). 

8Proof. To apply the first-order criteria, we take the partial derivative of ˆLmle with respect to A to obtain 

0 = ∂ ˆLmle 

∂A ˆAmle , ˆΣmle 

= − ˆΣ−1/2mle 

> N

X

> i=1

g ˆΣ−1/2mle 

 x′ 

> i

− ˆAmle xi

 x⊤ 

> i

,

which is equivalent to the first equation since ˆΣmle ∈ ΘΣ ⊆ S d

> ≻0

. Similarly, we take the partial derivative of ˆLmle with respect to Σ−1/2 (instead of Σ for the sake of simplicity) to obtain 

0 = ∂ ˆLmle 

∂Σ −1/2 ˆAmle , ˆΣmle 

= 1

N

> N

X

> i=1

hg ˆΣ−1/2mle 

 x′ 

> i

− ˆAmle xi

  x′ 

> i

− ˆAmle xi

⊤ + ˆΣ1/2mle 

i

= ˆΣ1/2mle + 1

N

PNi=1 g ˆΣ−1/2mle 

 x′ 

> i

− ˆAmle xi

  x′ 

> i

− ˆAmle xi

⊤,

where we apply formulae (C.1a) and (C.1c). The above characterization appears to be complicated for a generic ϕ-density family. Nevertheless, for elliptical families the solution can be expressed in simplified forms that better connects to the OLS estimator, as shown in the lemma below. 

Corollary 5 (MLE characterization, elliptical family) . For an elliptical family { fA,Σ }

with base density ϕ(w) = ψ(w⊤w), under Assumption 1 and the regularity assumption that PNi=1 ρi xi x⊤ 

> i

≻ 0, the MLE ( ˆ Amle , ˆΣmle ) satisfies the following system of equations: 



ˆAmle = P Ni=1 ρi x′ 

> i

x⊤

> i

P Ni=1 ρi xi x⊤

> i

−1

,

ˆΣmle = − 1

> N

PNi=1 ρi

 x′ 

> i

− ˆAmle xi

  x′ 

> i

− ˆAmle xi

⊤, (10) 

where 

ρi = ρ  x′ 

> i

− ˆAmle xi

⊤ ˆΣ−1mle 

 x′ 

> i

− ˆAmle xi

 

are dataset-specific coe ffi cients determined by the base density, where ρ(z) = 2ψ′(z) 

> ψ(z)

(see Lemma 1 for details). Proof. Plugging Lemma 1 into Lemma 4 gives the result. The above corollary clearly reveals the connection of MLE and the OLS estimator for elliptical families. On the one hand, MLE is in general di ff erent from the OLS estimator when ρi’s are non-degenerative (for a degenerative example, consider the case for Gaussian families, where ρi ≡ − 1 is constant and is thus canceled out). On the other hand, MLE for elliptical families take a similar form as OLS estimators, which can be viewed a reweighted version of the OLS formula. Indeed, since |ρ(z)| → 0 as 

z → +∞, coe ffi cients in (10) could be interpreted as reducing the weights of “outlier” noise samples that are significantly larger than the typical noise magnitude, which potentially enhances the convergence performance of the estimator. 

3.3. The Score-Matching Estimator (SME) 

In this section, we consider another type of estimators, i.e. the score-matching estimators (SME) first introduced in [27]: ( ˆ Asme , ˆΣsme ) : = arg min 

> A,Σ

ˆLsme (A, Σ ),

9where ˆ Lsme is selected as an empirical version of 

Lsme (A, Σ ) : = E(x,x′)∼dD



∇x′ log fA,Σ (x′|x) − ∇ x′ log fA⋆,Σ ⋆ (x′|x) 22



.

We point out that the expected loss Lsme is intractable in its current form, since the ground-truth density fA⋆,Σ ⋆ (x′|x) is involved. However, it is shown in [ 27 ] that the expected loss can be equivalently rewritten in a tractable form using the integration-by-part trick, as summarized by the following lemma. 

Lemma 6. Under Assumption 1 and 3, we have 

Lsme (A, Σ ) : = E(x,x′)∼dD

h ∇x′ log fA,Σ (x′|x) 22

+ 2 tr ∇2 

> x′

log fA,Σ (x′|x)i .

Proof. See Appendix A of [27] for details. Consequently, the SME with respect to a ϕ-density family { fA,Σ } with base density 

ϕ(w) can be defined as follows. 

(SME) ( ˆ Asme , ˆΣsme ) : = arg min 

> A,Σ

ˆLsme (A, Σ ), (11) where ˆLsme (A, Σ ) : =

> N

X

> i=1

h ∇x′ log fA,Σ (x′ 

> i

|xi) 22

+ 2 tr ∇2 

> x′

log fA,Σ (x′ 

> i

|xi)i 

=

> N

X

> i=1

h Σ−1/2gΣ−1/2(x′ 

> i

− Ax i) 22

+ 2 tr Σ−1HΣ−1/2(x′ 

> i

− Ax i)i .

Recall that g(·) and H(·) are defined in Assumption 1. Parallel to the previous section, we first show that the SME is unbiased, as summa-rized in the following lemma. 

Lemma 7 (consistency of SME) . Under Assumption 2, (A⋆, Σ ⋆) is the unique maximizer of Lsme (A, Σ ).Proof. This result is evident from the original definition of Lsme . On the one hand, 

Lsme is non-negative, with the minimum 0 achievable at ( A, Σ ) = (A⋆, Σ ⋆). On the other hand, for any ∇x′ log fA,Σ (x′|x) = ∇x′ log fA⋆,Σ ⋆ (x′|x), we must have log fA,Σ (x′|x) =

log fA⋆,Σ ⋆ (x′|x) + C, where C = 0 due to the unit normalization of densities. Finally, the proof is completed by applying Assumption 2. Parallel to the previous section, we can also establish asymptotic normality of SME as a corollary of the generic results (see, e.g., Corollary 1 in [ 28 ]). In fact, under As-sumption 1, 2 and some additional regularity assumptions of the score function, we can show that √N ˆθ − θ⋆

 d

→ N (0 , Ξ ), where Ξ :=  ∇2 

> θ

Lsme (θ⋆)−1(Pi, j Vi, j) ∇2 

> θ

Lsme (θ⋆)−1

is an instance-specific covariance matrix specified therein. However, the proof is purely technical and is thus omitted here. 10 One may expect a similar characterization lemma for SME. However, due to the complexity in its formulation, it is impractical to write down the equations. Fortu-nately, for elliptical families, we can still apply Lemma 1 to obtain a simple and neat characterization for SME parallel to Corollary 5. 

Lemma 8 (SME characterization, elliptical family) . For an elliptical family { fA,Σ }

with base density ϕ(w) = ψ(w⊤w), under Assumption 1 and the regularity assumption 

PNi=1(ρ2 

> i

+ 2τi)xi x⊤ 

> i

≻ 0, the SME ( ˆAsme , ˆΣsme ) satisfies the following system of equations: 



ˆAsme = P Ni=1(ρ2 

> i

+ 2τi)x′ 

> i

x⊤

> i

P Ni=1(ρ2 

> i

+ 2τi)xi x⊤

> i

−1

,

ˆΣsme = − 1

> N

PNi=1 

> ρ2
> i+2τi
> ρi

 x′ 

> i

− ˆAsme xi

  x′ 

> i

− ˆAsme xi

⊤, (12) 

where 

ρi = ρ  x′ 

> i

− ˆAsme xi

⊤ ˆΣ−1sme 

 x′ 

> i

− ˆAsme xi

 ,τi = τ  x′ 

> i

− ˆAsme xi

⊤ ˆΣ−1sme 

 x′ 

> i

− ˆAsme xi

 

are dataset-specific coe ffi cients determined by the base density, where ρ(z) = 2ψ′(z) 

> ψ(z)

and 

τ(z) = 4( ψ′′ (z)ψ(z)−ψ′(z)2) 

> ψ2(z)

(see Lemma 1). Proof. Plug Lemma 1 into ˆ Lsme (A, Σ ), and we have ˆLsme (A, Σ ) =

> N

X

> i=1

ρ2 

> i

Σ−1(x′ 

> i

− Ax i) 22 + 2ρi tr( Σ−1)

+ 2τi tr Σ−3/2(x′ 

> i

− Ax i)( x′ 

> i

− Ax i)⊤Σ−1/2

=

> N

X

> i=1

(ρ2 

> i

+ 2τi) Σ−1(x′ 

> i

− Ax i) 22 + 2ρi tr( Σ−1).

Therefore, to apply the first-order criteria, we take the partial derivative of ˆLsme with respect to A to obtain 

0 = ∂ ˆLsme 

∂A ˆAsme , ˆΣsme 

= 2 ˆ Σ−2sme 

> N

X

> i=1

(ρ2 

> i

+ 2τi) x′ 

> i

− ˆAsme xi

x⊤ 

> i

,

which is equivalent to the first equation since ˆΣsme ∈ ΘΣ ⊆ S d

> ≻0

. Similarly, we take the partial derivative of ˆLsme with respect to Σ−1 (instead of Σ for the sake of simplicity) to obtain 

0 = ∂ ˆLsme 

∂Σ −1 ˆAsme , ˆΣsme 

=

> N

X

> i=1

2( ρ2 

> i

+ 2τi) ˆ Σ−1sme 

 x′ 

> i

− ˆAsme xi

  x′ 

> i

− ˆAsme xi

⊤ + 2ρi I,

which is equivalent to the second equation. Note that here we apply formulae (C.1a) ,(C.1b) and (C.1d). We point out that the form of (12) is very similar to that of (10) , both regarded as reweighted versions of the OLS formula. 11 3.4. Unified Estimators for Gaussian Noise 

As briefly discussed above, it is intuitive to expect that the MLE and SME estimators coincide with the naive OLS estimator for the Gaussian family, which is indeed the case and can be easily verified as a sanity check in the following lemma. 

Lemma 9 (Unified Gaussian estimators) . For the Gaussian family (i.e., one with base density ϕ(x) = 1√2π exp  − x2

> 2

), under the regularity assumption that PNi=1 xi x⊤ 

> i

≻ 0, the MLE and SME estimators both coincide with the naive OLS estimator, i.e. 

( ˆ Amle , ˆΣmle ) = ( ˆ Asme , ˆΣsme ) = ( ˆ Aols , ˆΣols ).

Proof. Note that ρ(z) ≡ − 1 and τ(z) ≡ 0, and the results follows from Corollary 5 and Lemma 8 immediately. The above lemma also serves as a piece of supporting evidence that the MLE and SME estimators are supposed to outperform the naive OLS estimator by leveraging the shape information implicitly encoded in the ϕ-density family. On the contrary, since Gaussian distribution is the max-entropy distribution of prescribed mean and covariance, the estimators naturally degenerate into the naive OLS estimators due to a lack of additional structural information. 

3.5. Numerical Implementations 

In this section, we briefly discuss how to numerically compute the proposed OLS, MLE and SME estimators e ffi ciently. 

OLS. The OLS estimator can be computed either using the closed-form formula in (7) ,or by solving the optimization in (6) using a generic gradient-based solver, where the latter may be preferred for large datasets to avoid numerical issues. 

MLE and SME. In general, the MLE and SME estimators should be computed by solv-ing the optimization formulated in (9) and (11) , respectively, using a generic gradient-based solver. Alternatively, since MLE and SME for elliptical families are known to be reweighted OLS estimators in the form 

ˆA = P Ni=1 λi( ˆ A, ˆΣ)x′ 

> i

x⊤

> i

P Ni=1 λi( ˆ A, ˆΣ)xi x⊤

> i

−1

,

ˆΣ = − 1

> N

PNi=1 κi( ˆ A, ˆΣ) x′ 

> i

− ˆAx i

  x′ 

> i

− ˆAx i

⊤,

with weight functions λi(·) and κi(·) determined by data ( xi, x′ 

> i

), we may apply the following iterative algorithm that produces a sequence {( ˆ Ak, ˆΣk) | k = 1, 2, . . . }, where 

ˆAk+1 = P Ni=1 λi( ˆ Ak, ˆΣk)x′ 

> i

x⊤

> i

P Ni=1 λi( ˆ Ak, ˆΣk)xi x⊤

> i

−1

,

ˆΣk+1 = − 1

> N

PNi=1 κi( ˆ Ak, ˆΣk) x′ 

> i

− ˆAx i

  x′ 

> i

− ˆAx i

⊤.

It can be verified that the above iterative algorithm converges to the optimum for certain estimators and certain distributions (for example, MLE for the multivariate Student-t distribution, as shown in Section 4 below), but a general convergence guarantee is hard to establish. Curious readers are referred to Chapter 13 of [ 29 ] for a set of su ffi cient conditions that guarantee uniqueness and convergence of the iterative algorithm in the case without dynamics (i.e., the location parameter is simply μ instead of Ax i). We leave the detailed analysis of this algorithm as future work. 12 3.6. Single-trajectory Sample Complexity 

In this section, we present a single-trajectory sample complexity analysis for the proposed MLE and SME estimators in the case where the system is stable and the noise is sub-Gaussian. 

Assumption 3 (Single-trajectory data) . D contains transitions collected from a single trajectory; i.e., we have D = {(xi, xi+1) | t ∈ [N]} for a trajectory x0, x1, x2, . . . subject to the dynamics xi = A⋆ xi−1 + Σ1/2 

> ⋆

wi−1, ∀i ∈ N.

Assumption 4 (Sub-Gaussian noise) . The noise wi is i.i.d. sub-Gaussian with variance proxy σ2 (see Definition 2). 

Assumption 5 (Stability) . The system is stable in the sense that ρ(A⋆) < 1, and that the Gramian Γt(A) : = Pt−1 

> τ=0

Aτ(Aτ)⊤ is bounded by Γ. Consequently, ∥xi∥ ≤ Cx, ∀i ∈ [N]. 

Assumption 6 (Bounded Θ). The hypothesis space Θ is bounded such that ∥A∥ ≤ CA,

∀A ∈ ΘA and ∥Σ−1/2∥ ≤ CΣ , ∀Σ ∈ ΘΣ .

Theorem 10 (Sample Complexity) . Under Assumptions 1, 2, 3, 4, 5 and 6, when 

( ˆ A, ˆΣ) ∈ ( ˆ Amle , ˆΣmle ), ( ˆ Asme , ˆΣsme ) , we have with probability at least 1 − δ

ˆA − A⋆ ≲

r d log( d/δ )

N ,

ˆΣ − Σ⋆ ≲

r d log( dN /δ ) log(1 /δ )

N .

Here the asymptotics hide instance-specific constants that do not depend on d, N or δ.Proof sketch. Given the reweighted OLS characterization for MLE and SME estimators (see Corollary 5 and 8), we shall first apply known self-normalized martingale con-centration bounds (see [ 30 ]) to derive the convergence rate of A. To further derive the convergence rate of Σ, we may resort to the concentration of ˆL around L, as well as the smoothness and strong convexity of the objective function over a bounded hypothesis space. Detailed proofs can be found in Appendix B. The sample complexity guarantee shows that the proposed estimators achieves a convergence rate of ˜ O( N−1/2), which is identical to standard MLE estimators. 

4. Simulations 

In this section, we examine the numerical performance of the proposed MLE and SME estimators via simulation. We start by comparing the identification error under di ff erent sample sizes to provide empirical evidence for the sample complexity results. Then we compare the identification error under di ff erent system dimensions to showcase the scalability of the algorithms. We also evaluate the proposed estimators in terms of computation time to reflect their computational complexity, highlighting a trade-o ff 

between sample and computational complexity. 13 Figure 1: Identification error of MLE, SME and OLS estimators for di ff erent sample sizes (2-d Student-t family, log scale, shadowed confidence interval). 

Setting. We consider the Student-t family with base density ϕt(w) selected as the multivariate Student-t distribution, as specified in Example 2. For simulations in a 2-d system with di ff erent sample sizes, the following system parameters are used: 

A⋆ =

"1 2

> 12

#

, Σ⋆ =

"14

#

.

For simulations on scalability with di ff erent system dimensions, the dynamical matrix 

A⋆ is a tri-diagonal matrix, while Σ⋆ is simply selected as identity, i.e. 

A⋆ =



1 11 1 11 1 1  

> . . .. . .. . .



> d×d

, Σ⋆ = Id.

All optimizations are solved by the general-purpose nonlinear solver provided by SciPy [ 31 ] using the BFGS algorithm [ 32 ]. Experiments are repeated for 100 times to compute the median and confidence interval (marked with shadow). 

Sample Complexity. To empirically evaluate the sample complexity of the MLE, SME and naive OLS estimators, we first compare their identification error in the 2-d system specified above. It is shown in Figure 1 below that the estimation errors of all estimators converge to 0 as the number of samples grow, yet at di ff erent rates and displaying di ff erent patterns. When examining the curves closely, it can be observed that MLE has the smallest identification error in all settings, both in terms of the median and the confidence interval; further, while the estimation error of A convergence at similar rates for all the three estimators, their performance for identifying Σ di ff ers significantly, with OLS the worst, MLE the best, and SME in between. These observations match well with our initial motivation that better leveraging the “shape” information of the distribution family helps to improve the identification performance. 

Scalability. To reveal the scalability of the proposed estimators, we also measure the estimation error of the MLE and OLS estimators with di ff erent system dimensions using a fixed number of samples (256 in implementation), with SME excluded since the solver fails to output solutions for higher dimensions within reasonable time. As shown in Figure 2 below, it can be observed that the advantage of MLE over the naive 14 Figure 2: Identification error of MLE and OLS estimators for di ff erent dimensions (256 samples, log scale, shadowed confidence interval). 

OLS estimator becomes more significant as the system dimension, which can in turn be attributed to the increased dissimilarity of the Student-t family from the Gaussian family in higher dimensions. 

Computational E ffi ciency. We proceed to evaluate the computational e ffi ciency of the proposed estimators under di ff erent sample sizes and di ff erent system dimensions. For this purpose, we include two di ff erent implementations of the MLE estimator—the standard implementation by solving the optimization, and the iterative implementation (labeled as “ iter ”) by applying the iterative algorithm proposed in Section 3.5. It is shown in Figure 3 that the iterative algorithm is able to approximate the MLE to a very high precision after only 20 iterations. The average computation time are then measured and shown in Figure 4. It can be observed in Figure 4a that the computation of MLE and SME is significantly less effi cient than the naive OLS estimator, where the latter comes with a closed-form formula and thus does not involve any optimization. Further, the computation time for MLE is reduced by approximately an order of magnitude when the iterative algorithm is applied, justifying the e ffi ciency of the iterative algorithm. A similar slowdown of MLE computation (as compared to the naive OLS baseline) is observed in Figure 4b, which deteriorates with higher system dimensions. We point out again that SME is not included in Figure 4b because the solver simply fails to minimize ˆLsme in higher dimensions, probably due to the highly non-convex landscape of the optimization problem. A comparison of Figure 2 and Figure 4b reveals a clear trade-o ff between sample complexity and computational e ffi ciency. 

Mis-specified Base Density. Finally, we empirically illustrate the proposed estimators’ robustness with respect to mis-specified base densities. Consider the case where the student-t density is perturbed, such that the w⊤w term is replaced by ( w⊤w)1+ε, i.e. log ˜ ϕε 

> t

(w) = − ν + d

2Zε

log 1 + 1

ν − 2 (w⊤w)1+ε

!

,

where Zε is an appropriate normalizing factor for error level ε. The estimation errors at di ff erent error levels are plotted in the following Figure 5. It can be observed that ˆAmle slightly outperforms ˆAols in a consistent manner, while the prediction error of ˆΣmle 

gradually deteriorates with larger ε, and eventually becomes worse than ˆ Σols .15 Figure 3: Identification error of di ff erent implementations of MLE for di ff erent sample sizes (2-d Student-t family, log scale, shadowed confidence interval).  

> (a) 2-d system, di ff erent sample sizes (b) 256 samples, di ff erent system dimensions
> Figure 4: Average computation time of MLE, SME and OLS estimators for di ff erent dimensions (Student-t family, log scale).

Such deterioration in performance is quite intuitive, and also matches with our theoretical analysis. In fact, when the error ∥ ˜ϕ(w) − ϕ(w)∥ is supposed to be bounded, following the derivations presented in Appendix B, we will see an additional drift term in the prediction error bound that positively correlates to the error level ε. The above discussion shows that the proposed estimators exhibit certain level of robustness to mis-specified base densities when the error level is mild. 

5. Conclusion 

In this paper, we study sample-e ffi cient joint identification of linear dynamics and noise covariance for generic noise distributions via a novel parameterization of the state-transition distribution. Building upon both theoretical and experimental evidence, we highlight the necessity to leverage the “shape” information of distributions that is implicitly encoded in the ϕ-density families, and propose two practical estimators (i.e., MLE and SME) to solve the joint identification problem, along with characterization of their key properties and a sample complexity guarantee. Simulation results confirms 16 Figure 5: Identification error of MLE estimators for mis-specified base densities at di ff erent error levels (2-d perturbed Student-t family, log scale, shadowed confidence interval). 

the outperformance of the proposed estimators against the naive OLS baseline at higher computational costs, showing a trade-o ff between sample complexity and computational effi ciency. Future work includes discovering better estimators to account for a wider range of distribution families and designing more e ffi cient algorithms to compute the estimators. 

Acknowledgement 

This paper is supported by the following grants: NSF ECCS 2328241 (ASCENT), NSF CBET 2112085 (AI Institute), NSF ECCS 2401390, ONR N000142512173. 

References 

[1] L. Ljung, Consistency of the least-squares identification method, IEEE Transac-tions on Automatic Control 21 (1976) 779–781. [2] L. Ljung, On the consistency of prediction error identification methods, in: Mathematics in Science and Engineering, volume 126, Elsevier, 1976, pp. 121– 164. [3] M. Simchowitz, H. Mania, S. Tu, M. I. Jordan, B. Recht, Learning without mixing: Towards a sharp analysis of linear system identification, in: Conference on Learning Theory, PMLR, 2018, pp. 439–473. [4] T. Sarkar, A. Rakhlin, Near optimal finite time identification of arbitrary linear dynamical systems, in: International Conference on Machine Learning, PMLR, 2019, pp. 5610–5618. [5] S. Oymak, N. Ozay, Non-asymptotic identification of LTI systems from a single trajectory, in: 2019 American Control Conference (ACC), IEEE, 2019, pp. 5655– 5661. 17 [6] Y. Jedra, A. Proutiere, Finite-time identification of linear systems: Fundamental limits and optimal algorithms, IEEE Transactions on Automatic Control 68 (2022) 2805–2820. [7] A. Cohen, T. Koren, Y. Mansour, Learning linear-quadratic regulators e ffi ciently with only √T regret, in: International Conference on Machine Learning, PMLR, 2019, pp. 1300–1309. [8] H. Mania, S. Tu, B. Recht, Certainty equivalence is e ffi cient for linear quadratic control, Advances in Neural Information Processing Systems 32 (2019). [9] S. Dean, H. Mania, N. Matni, B. Recht, S. Tu, On the sample complexity of the linear quadratic regulator, Foundations of Computational Mathematics 20 (2020) 633–679. [10] M. Simchowitz, D. Foster, Naive exploration is optimal for online LQR, in: International Conference on Machine Learning, PMLR, 2020, pp. 8937–8948. [11] S. Zymler, D. Kuhn, B. Rustem, Distributionally robust joint chance constraints with second-order moment information, Mathematical Programming 137 (2013) 167–198. [12] B. P. Van Parys, D. Kuhn, P. J. Goulart, M. Morari, Distributionally robust control of constrained stochastic systems, IEEE Transactions on Automatic Control 61 (2015) 430–442. [13] M. Kishida, A. Cetinkaya, Risk-aware linear quadratic control using conditional value-at-risk, IEEE Transactions on Automatic Control 68 (2023) 416–423. [14] Y. Hu, S. Talebi, N. Li, Risk-sensitive a ffi ne control synthesis for stationary LTI systems, arXiv preprint arXiv:2410.17581 (2024). [15] Y. Huang, Y. Zhang, Z. Wu, N. Li, J. Chambers, A novel adaptive Kalman filter with inaccurate process and measurement noise covariance matrices, IEEE Transactions on Automatic Control 63 (2017) 594–601. [16] J. O. d. A. Limaverde Filho, E. L. Fortaleza, J. Silva, M. de Campos, Adap-tive Kalman filtering for closed-loop systems based on the observation vector covariance, International Journal of Control 95 (2022) 1731–1746. [17] R. Mehra, On the identification of variances and adaptive Kalman filtering, IEEE Transactions on Automatic Control 15 (1970) 175–184. [18] B. J. Odelson, M. R. Rajamani, J. B. Rawlings, A new autocovariance least-squares method for estimating noise covariances, Automatica 42 (2006) 303–308. [19] M. Ge, E. C. Kerrigan, Noise covariance identification for time-varying and nonlinear systems, International Journal of Control 90 (2017) 1903–1915. 18 [20] V. A. Bavdekar, A. P. Deshpande, S. C. Patwardhan, Identification of process and measurement noise covariance for state and parameter estimation using extended Kalman filter, Journal of Process Control 21 (2011) 585–601. [21] M. A. Zagrobelny, J. B. Rawlings, Identifying the uncertainty structure using maximum likelihood estimation, in: 2015 American Control Conference (ACC), IEEE, 2015, pp. 422–427. [22] J. Shao, Mathematical statistics, Springer Science & Business Media, 2008. [23] S. Cambanis, S. Huang, G. Simons, On the theory of elliptically contoured distributions, Journal of Multivariate Analysis 11 (1981) 368–385. [24] H. Teicher, Maximum likelihood characterization of distributions, The Annals of Mathematical Statistics 32 (1961) 1214–1222. [25] A. W. Marshall, I. Olkin, Maximum likelihood characterizations of distributions, Statistica Sinica (1993) 157–171. [26] L. Wasserman, All of statistics: a concise course in statistical inference, Springer Science & Business Media, 2013. [27] A. Hyv ¨arinen, P. Dayan, Estimation of non-normalized statistical models by score matching, Journal of Machine Learning Research 6 (2005). [28] Y. Song, S. Garg, J. Shi, S. Ermon, Sliced score matching: A scalable approach to density and score estimation, in: Uncertainty in Artificial intelligence, PMLR, 2020, pp. 574–584. [29] M. Bilodeau, D. Brenner, Theory of Multivariate Statistics, Springer Science & Business Media, 1999. [30] Y. Abbasi-Yadkori, D. P ´al, C. Szepesv ´ari, Improved algorithms for linear stochastic bandits, Advances in Neural Information Processing Systems 24 (2011). [31] P. Virtanen, R. Gommers, T. E. Oliphant, M. Haberland, T. Reddy, D. Cournapeau, E. Burovski, P. Peterson, W. Weckesser, J. Bright, et al., SciPy 1.0: Fundamental algorithms for scientific computing in Python, Nature Methods 17 (2020) 261–272. [32] R. Fletcher, Practical Methods of Optimization, John Wiley & Sons, 2000. [33] K. B. Petersen, M. S. Pedersen, et al., The Matrix Cookbook, Technical University of Denmark 7 (2008) 510. 

Notations. Let Sn denote the set of all n-by-n symmetric matrices, where subscripts indicate positive-(semi)definiteness. Let A ≻ B (A ≽ B) be a shorthand for denoting 

A − B to be positive (semi)definite. Let ∥·∥ denote the Euclidean 2-norm for vectors and the induced 2-norm for matrices. Let E[X] and Var[ X] denote the expectation and the covariance matrix of a vector-valued random variable X, respectively. Let det (A) denote the determinant of A. Let ∇x f (x, y) denote the partial derivative of f with respect to x,where x and y are allowed to be vectors. 19 Appendix A. Proof of Lemma 1 

Proof of Lemma 1. By direct computation we have 

g(w) : = ∇w log ϕ(w) = ∇w log ψ(w⊤w)

= 2 ddz [log ψ(z)] z=w⊤w

w

= ρ(w⊤w)w,

where we apply (C.1f) and the definition of ρ(z). Similarly, 

H(w) : = ∇2 

> w

log ϕ(w)

= 2 ddz [log ψ(z)] z=w⊤w

· I + 4 d2

dz2 [log ψ(z)] 

> z=w⊤w

· ww ⊤

= ρ(w⊤w)I + τ(w⊤w)ww ⊤,

where we apply (C.1g) and the definitions of ρ(z) and τ(z). 

Appendix B. Sample Complexity Analysis 

In this section, we present our sample complexity analysis in a generic way. Specifi-cally, consider any unbiased expected loss L(A, Σ ) = Ehℓ Σ−1/2(x′ − Ax ), Σ i , i.e., (A⋆, Σ ⋆) = arg min  

> A,Σ

L(A, Σ ).

The corresponding estimator for ( A, Σ ) is constructed by minimizing its empirical version, namely ( ˆ A, ˆΣ) : = arg min 

> A,Σ

ˆL(A, Σ ),

where ˆL(A, Σ ) : = 1

N

> N

X

> i=1

ℓ Σ−1/2(x′ 

> i

− Ax i), Σ .

In this section, we make the following assumption on ℓ.

Assumption 7 (Well-conditioned losses) . ℓ(w, Σ ) is L-smooth in w, and μ-strongly convex in both w and Σ; ℓ(0 , Σ ) = 0, ∀Σ ∈ ΘΣ , and Ew∼ϕ[ℓ(w, Σ )] ≤ Cℓ.In addition, prompted by Corollary 5 and Lemma 8, we also assume that the estimator ˆA can be expressed in a reweighted OLS form, as summarized in the following assumption. 

Assumption 8 (Reweighted OLS form) . There exist weights λi ∈ (λ−, λ +) such that ˆA = P Ni=1 λi x′ 

> i

x⊤

> i

P Ni=1 λi xi x⊤

> i

−1

.We point out that assuming bounded instance-specific weights λi’s should not be deemed as restrictive in the following sense. In practice, we usually have an initial guess of ( A, Σ ), especially when we have some prior knowledge of the system. When the hypothesis space Θ is selected as a su ffi ciently neighborhood around ( A⋆, Σ ⋆), it can be shown that such bounds λ+ > λ − > 0 exist with high probability, owing to the continuity of the solution in A and Σ.The following lemma specifies the sample complexity bound for estimating A using data collected from a single trajectory. 20 Lemma 11. Under Assumptions 3, 5, 7 and 8, given any δ ∈ (0 , 12 ), there exists an N0 ∈ N where N0 = ˜O  ∥Σ1/2 

> ⋆

∥σ2d log 1

> δ

, such that for any N > N0, with probability at least 1 − δ,

ˆA − A⋆ ≲

r d log( d/δ )

N .

Proof. It is straight-forward to bound the error as follows: 

ˆA − A⋆ =



> N

X

> i=1

λi x′ 

> i

x⊤

> i



> N

X

> i=1

λi xi x⊤

> i



> −1

− A⋆ (B.1a) 

=



> N

X

> i=1

λi(x′ 

> i

− A⋆ xi)x⊤

> i



> N

X

> i=1

λi xi x⊤

> i



> −1

(B.1b) 

=



> N

X

> i=1

λi ˜wi x⊤

> i



> N

X

> i=1

λi xi x⊤

> i



> −1

(B.1c) 

≤ λ+

λ−



> N

X

> i=1

˜wi x⊤

> i



> N

X

> i=1

xi x⊤

> i



> −1

(B.1d) 

≤ λ+

λ−



> N

X

> i=1

˜wi x⊤

> i



> N

X

> i=1

xi x⊤

> i



> −1/2

·



> N

X

> i=1

xi x⊤

> i



> −1/2

(B.1e) 

= λ+

λ−

·

P Ni=1 ˜wi x⊤

> i

P Ni=1 xi x⊤

> i

−1/2

σmin 

P Ni=1 xi x⊤

> i

1/2 , (B.1f) where in (B.1c) we introduce the scaled noise ˜wi := Σ1/2 

> ⋆

wi = x′ 

> i

− A⋆ xi, which is sub-Gaussian with variance proxy  ∥Σ1/2 

> ⋆

∥σ2 by Lemma 15; in (B.1d) we use the facts that PNi=1 λi xi x⊤ 

> i

≽ λ−

PNi=1 xi x⊤ 

> i

when λi ≥ λ− (∀i ∈ [N]), and that ∥AB ∥ ≥ ∥ AC ∥ for any A when B ≽ C are both symmetric; in (B.1e) we apply the sub-multiplicativity of matrix norms. Now it only su ffi ces to bound the numerator and denominator in (B.1f) . For the numerator, using Lemma 16, we know that with probability at least 1 −δ, with su ffi ciently large N ≥ N0, where N0 = ˜O  ∥Σ1/2 

> ⋆

∥σ2d log 1

> δ

, we have 

> N

X

> i=1

xi x⊤ 

> i

≽

 ∥Σ1/2 

> ⋆

∥σ2

4 NI ,

and thus 

σmin 



> N

X

> i=1

xi x⊤

> i

 ≥

 ∥Σ1/2 

> ⋆

∥σ2

4 N.

Meanwhile, the denominator is bounded by Lemma 17, so that 

ˆA − A⋆ ≲ 2λ+

∥Σ1/2 

> ⋆

∥σλ −

r d log( d/δ )

N .

This completes the proof. 21 Lemma 12. Under the premises of Lemma 11, and Assumptions 4, 5 and 6, for any 

δ ∈ (0 , 1) , with probability at least 1 − δ,

ˆL(A, Σ ) − L (A, Σ ) ≲ d log( dN /δ ) log(1 /δ )

N .

Proof. We start by decomposing the error into three terms ˆL(A, Σ ) − L (A, Σ ) = ˆL(A, Σ ) − ˆL(A⋆, Σ )

+ ˆL(A⋆, Σ ) − L (A⋆, Σ )

+ L(A⋆, Σ ) − L (A, Σ ).

For the first term, with probability at least 1 − δ/ 4, we have 

ˆL(A, Σ ) − ˆL(A⋆, Σ )

= 1

N

> N

X

> i=1

ℓ Σ−1/2(x′ 

> i

− Ax i), Σ  − ℓ Σ−1/2(x′ 

> i

− A⋆ xi), Σ  

≤ 12 L Σ1/2(A − A⋆)xi 

> 2

≤ 12 L∥Σ1/2∥2∥A − A⋆∥2∥xi∥2

≤ 12 LC 2

> x

C2 

> Σ

∥A − A⋆∥2 ≲ d log( d/δ )

N ,

where we plug in Assumption 6, Assumption 7 and Lemma 11. Similarly, for the third term, with probability at least 1 − δ/ 4, 

L(A, Σ ) − L (A⋆, Σ ) ≤ 12 LC 2

> x

C2 

> Σ

∥A − A⋆∥2 ≲ d log( d/δ )

N .

For the second term, note that with the true A⋆, Σ−1/2 

> ⋆

(x′ 

> i

− A⋆ xi) = wi are i.i.d. samples from the base distribution of zero mean. Therefore, using Lemma 14, we have 

∥wi∥ ≤ 

r

2cσ2 log 8N

δ ,

with probability at least 1 − δ/ (4 N). Consequently, 

ℓ(wi, Σ ) − Ew[ℓ(w, Σ )] ≤ ℓ(wi, Σ ) − ℓ(0 , Σ ) + Ew[ℓ(w, Σ )] 

≤ Lc σ2 log 8N

δ + Cℓ

by Assumption 7. Finally, using Hoe ff ding’s inequality, we have 

ˆL(A⋆, Σ ) − L (A⋆, Σ ) ≤ 1

N

> N

X

> i=1

ℓ(wi, Σ ) − Ew[ℓ(w, Σ )] 

≤ 2Lc σ2

N log 8N

δ log 8

δ + 2Cℓ

N log 8

δ

with probability at least 1 − δ/ 4. The proof is now completed by applying union bound to the inequalities above. 

Lemma 13. Under the premises of Lemma 12, for any δ ∈ (0 , 1) , with probability at least 1 − δ, we have 

ˆΣ − Σ⋆ ≲

r d log( dN /δ ) log(1 /δ )

N .

Proof. Note that, by definition of ( ˆ A, ˆΣ) and ( A⋆, Σ ⋆), we have ˆL( ˆ A, ˆΣ) ≥ ˆL(A⋆, Σ ⋆), L(A⋆, Σ ⋆) ≥ L ( ˆ A, ˆΣ).

22 Therefore, we shall apply Lemma 12 twice to obtain 0 ≤ ˆL( ˆ A, ˆΣ) − ˆL(A⋆, Σ ⋆) + L(A⋆, Σ ⋆) − L ( ˆ A, ˆΣ)

≤ ˆL( ˆ A, ˆΣ) − L ( ˆ A, ˆΣ) + L(A⋆, Σ ⋆) − ˆL(A⋆, Σ ⋆)

≲ 2 · d log( dN /δ ) log(1 /δ )

N

with probability at least 1 − δ. Meanwhile, by μ-strong convexity assumed in Assump-tion 7, we have in the opposite direction, ˆL( ˆ A, ˆΣ) − ˆL(A⋆, Σ ⋆) ≥ μ

2 ∥ ˆΣ − Σ⋆∥2,

L(A⋆, Σ ⋆) − L ( ˆ A, ˆΣ) ≥ μ

2 ∥ ˆΣ − Σ⋆∥2.

Combining these inequalities yields an upper bound on ˆΣ − Σ⋆ :

ˆΣ − Σ⋆ ≲

r d log( dN /δ ) log(1 /δ )

N .

This completes the proof. To conclude Theorem 10 as direct corollaries of Lemma 11 and Lemma 13, it only su ffi ces to verify Assumptions 7 and 8, which is straight-forward due to the boundedness of Θ and the guaranteed finite first-order moment of the base distribution. 

Appendix C. Technical Tools Matrix Di ff erential Calculus. The following formulae for di ff erentiating matrix-variate functions are well-known in literature (see, e.g., [33]) and can be easily verified. 

∂ log det( A)

∂A = A−1, (C.1a) 

∂ tr( A)

∂A = I, (C.1b) 

∂ f (CAx )

∂A = C⊤[∇ f (CAx )] x⊤, (C.1c) 

∂ f (Ax + b)

∂x = A⊤[∇ f (Ax + b)] , (C.1d) 

∂2 f (Ax + b)

∂x2 = A⊤[∇2 f (Ax + b)] A, (C.1e) 

∂ f (x⊤ x)

∂x = 2 f ′(x⊤ x)x, (C.1f) 

∂2 f (x⊤ x)

∂x2 = 2 f ′(x⊤ x)I + 4 f ′′ (x⊤ x)xx ⊤. (C.1g) 

Sub-Gaussian Random Variable. Sub-Gaussian random variables, a notion that intuitively captures distributions with tails “lighter” than Gaussian, shall be defined as follows. 

Definition 2 (Sub-Gaussian) . A random vector x ∈ Rd is said to be sub-Gaussian with 

variance proxy σ2, if 

Ehexp  v⊤(x − μ)ti ≤ exp  σ2t2/2, ∀t ∈ R, ∀v ∈ Sd−1,

where μ := E[x] is the expectation of x.23 As its name suggests, sub-Gaussian random variables have tails “lighter” than Gaussian, as summarized below. 

Lemma 14. Given a sub-Gaussian random vector x ∈ Rd with variance proxy σ2, we have 

Pr ∥x − μ∥ ≥ t ≤ 2 exp − t2

2cσ2

!

for some universal constant c, where μ := E[x].

The following lemma guarantees that the linear transform of a sub-Gaussian random variable is still sub-Gaussian. 

Lemma 15. Given a sub-Gaussian random vector x ∈ Rd with variance proxy σ2, for any A ∈ Rd×d, Ax is also a sub-Gaussian random vector, with variance proxy  ∥A∥σ2.Proof. Let μ := E[x]. Then for any t ∈ R and v ∈ Sd−1 we have 

Ehexp  v⊤(Ax − E[Ax ]) ti = Ehexp  (v⊤ A(x − μ)ti 

= E



exp 

 A⊤v

> ∥A⊤v∥

⊤

(x − μ) · (∥A⊤v∥t)

 

≤ exp  σ2(∥A⊤v∥t)2/2

≤ exp  (∥A∥σ)2t2/2,

where in the last inequality we use the fact that ∥A⊤v∥ ≤ ∥ A∥.

Concentration Bounds for Time Series. The following auto-regressive concentration bounds are established in [4]. 

Lemma 16. Suppose zi+1 = A⋆zi + ηi, where ρ(A⋆) < 1 is stable, and {ηi} is i.i.d. sub-Gaussian with variance proxy σ2. Then, with probability at least 1 − δ, there exists some N0 = ˜Oσ2d log 1

> δ

,

such that for any N > N0, we have NX

> i=1

ziz⊤ 

> i

≽ σ2 N

4 I.

Proof. See Section 9 in [4]. 

Lemma 17. Suppose zi+1 = A⋆zi + ηi, where ρ(A⋆) < 1 is stable, and {ηi} is i.i.d. sub-Gaussian with variance proxy σ2. Further, suppose the Gramian Γt(A) : = Pt−1 

> τ=0

Aτ(Aτ)⊤

is bounded by Γ. Then, with probability at least 1 − δ, we have 



> N

X

> i=1

ηiz⊤

> i



> N

X

> i=1

ziz⊤

> i



> −1/2

≲ pd log( d/δ ).

Proof. For simplicity, let S := PNi=1 ηiz⊤ 

> i

and Y := PNi=1 ziz⊤ 

> i

. Define λ := σmin (Y), such that ¯ Y := Y + λI ≼ 2Y. Then by Proposition 8.2 in [4] we have 

S Y −1/2 ≤ √2 S ¯Y−1/2 ≤ 4σ

vuuut

d log 



5 det( ¯ Y)det( λI)

1/2d

δ

.

24 Meanwhile, by Proposition 8.4 in [4] we also have 

∥Y∥ ≤ tr 



> N

X

> i=1

Γt(A)

 1 + 1

C log 1

δ

!

≤ dN Γ 1 + 1

C1

log 1

δ1/d

!

for some absolute constant C1. Consequently, by Lemma 16, det( ¯ Y)det( λI) ≤ 2 ∥Y∥

λ

!d

≤ 2



4dΓ1 + 1 

> C1

log 1

> δ



σ2



> d

,

and thus 

S Y −1/2 ≤ 4σ

s

d

2 log Cd log(1 /δ )

δ

!

≍ pd log( d/δ ).

for some absolute constant C.25