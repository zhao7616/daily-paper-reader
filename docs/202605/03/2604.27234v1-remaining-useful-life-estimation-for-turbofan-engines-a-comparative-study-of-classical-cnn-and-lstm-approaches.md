---
title: "Remaining Useful Life Estimation for Turbofan Engines: A Comparative Study of Classical, CNN, and LSTM Approaches"
title_zh: 涡扇发动机剩余使用寿命估计：经典方法、CNN 和 LSTM 方法的对比研究
authors: "Astitva Goel, Samarth Galchar, Sumit Kanu"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.27234v1"
tags: ["query:rul"]
score: 10.0
evidence: 使用CNN和LSTM对涡扇发动机进行剩余寿命（RUL）估计
tldr: 本研究在NASA C-MAPSS数据集上对比了经典回归、1D CNN和LSTM模型在涡扇发动机剩余使用寿命（RUL）预测中的表现。实验证明，简单的单层LSTM架构在预测精度上优于复杂的深度LSTM，而XGBoost在特定子集上也展现出极强的非线性建模能力。该研究为工业设备预防性维护提供了模型选择依据和公平的性能基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.27234v1/fig-001.webp\", \"caption\": \"Fig. 7 LSTM hidden state evolution for a single engine trajectory on FD001 (left) and FD003 (right). FD001 units show clear directional transitions toward failure while FD003 units exhibit more varied patterns reflecting its two fault conditions.\", \"page\": 6, \"index\": 1, \"width\": 1076, \"height\": 222}, {\"url\": \"assets/figures/arxiv/2604.27234v1/fig-002.webp\", \"caption\": \"TABLE III Test Results on FD001 and FD003\", \"page\": 4, \"index\": 2, \"width\": 848, \"height\": 205}, {\"url\": \"assets/figures/arxiv/2604.27234v1/fig-003.webp\", \"caption\": \"Fig. 1 LSTM training and validation MSE loss over epochs for FD001 (left) and FD003 (right).\", \"page\": 4, \"index\": 3, \"width\": 1076, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2604.27234v1/fig-004.webp\", \"caption\": \"Fig. 2 CNN training and validation MSE loss over epochs for FD001 (left) and FD003 (right). High initial validation spikes stabilize rapidly as the convolutional filters adapt to sensor degradation patterns.\", \"page\": 4, \"index\": 4, \"width\": 1076, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2604.27234v1/fig-005.webp\", \"caption\": \"Fig. 4 Predicted versus true RUL for the CNN on the FD001 (left) and FD003 (right) test sets.\", \"page\": 5, \"index\": 5, \"width\": 1076, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2604.27234v1/fig-006.webp\", \"caption\": \"Fig. 3 Predicted versus true RUL for the LSTM on the FD001 (left) and FD003 (right) test sets.\", \"page\": 5, \"index\": 6, \"width\": 1076, \"height\": 206}]"
motivation: 旨在通过对比不同复杂度的机器学习模型，提升涡扇发动机剩余使用寿命预测的准确性以优化维护调度。
method: 在统一预处理流程下，对比评估了Ridge回归、XGBoost、1D CNN及单层LSTM在C-MAPSS数据集上的表现。
result: LSTM模型在多个子集上取得了最低的均方根误差，且XGBoost在FD003数据集上表现出显著的非线性建模优势。
conclusion: 简单的循环神经网络架构在RUL预测中极具效率，且传统非线性模型在特定工业场景下依然保持高度竞争力。
---

## 摘要
剩余使用寿命（RUL）估计是故障预测与健康管理（PHM）的关键组成部分，能够实现主动维护调度并减少工业设备的非计划故障。本文针对 NASA C-MAPSS 涡扇发动机数据集，对用于 RUL 估计的机器学习方法进行了对比研究，包括：经典基准模型（岭回归、多项式岭回归和 XGBoost）、一维卷积神经网络（1D CNN）以及长短期记忆网络（LSTM）。所有模型均在相同的预处理流程下，在 FD001 和 FD003 子集上进行评估，以确保公平比较。在原始序列模型中，LSTM 在 FD001 和 FD003 上分别实现了 14.93 和 14.20 的均方根误差（RMSE），尽管采用了更简单的单层架构，但其表现优于 Zheng 等人报道的深度 LSTM（RMSE 分别为 16.14 和 16.18）。1D CNN 在 FD001 和 FD003 上的 RMSE 分别为 16.97 和 15.68，在 FD003 上表现出具有竞争力的性能，而在 FD001 上产生的 RUL 预测则更为保守。岭回归在原始特征和工程特征上均进行了评估，而其他经典模型仅使用工程输入。XGBoost 在 FD003 上实现了 13.36 的 RMSE，突显了非线性建模的竞争力。

## Abstract
Remaining Useful Life (RUL) estimation is a critical component of Prognostics and Health Management (PHM), enabling proactive maintenance scheduling and reducing unplanned failures in industrial equipment. This paper presents a comparative study of machine learning approaches for RUL estimation on the NASA C-MAPSS turbofan engine dataset: classical baselines (Ridge Regression, Polynomial Ridge, and XGBoost), a 1D Convolutional Neural Network (CNN), and a Long Short-Term Memory (LSTM) network. All models are evaluated on the FD001 and FD003 subsets under an identical preprocessing pipeline to ensure a fair comparison. Among raw-sequence models, the LSTM achieves RMSE of 14.93 and 14.20 on FD001 and FD003 respectively, outperforming the deep LSTM reported by Zheng et al.~\cite{paper} (RMSE 16.14 and 16.18) despite using a simpler single-layer architecture. The 1D CNN achieves RMSE of 16.97 on FD001 and 15.68 on FD003, demonstrating competitive performance on FD003 while producing more conservative RUL predictions on FD001. Ridge Regression is evaluated on raw and engineered features, while other classical models use only engineered inputs. XGBoost achieves an RMSE of 13.36 on FD003, highlighting the competitiveness of nonlinear modeling.

---

## 论文详细总结（自动生成）

这是一份关于论文《Remaining Useful Life Estimation for Turbofan Engines: A Comparative Study of Classical, CNN, and LSTM Approaches》的结构化总结：

### 1. 核心问题与整体含义
本文的研究核心是工业设备（特别是航空涡扇发动机）的**剩余使用寿命（RUL）预测**。在预测性维护领域，准确预测设备失效前的剩余循环次数对于减少非计划停机、降低维护成本和保障安全至关重要。研究动机在于：尽管深度学习在 RUL 预测中表现优异，但不同模型（经典机器学习与深度学习）在相同预处理条件下的公平对比仍较少。本文旨在通过统一的实验框架，评估不同复杂度模型在处理传感器时间序列数据时的效能。

### 2. 方法论
论文提出了一套标准化的预处理流程，并对比了三类模型：
*   **预处理核心技术：**
    *   **分段线性 RUL 目标：** 将早期健康阶段的 RUL 设定为固定值（130），以避免模型在设备未退化时学习虚假的退化信号。
    *   **传感器筛选与标准化：** 剔除方差接近零的传感器，采用 Z-score 标准化。
    *   **滑动窗口：** 使用长度为 30 的滑动窗口提取时间序列特征。
*   **模型架构：**
    *   **经典模型：** 包括岭回归（Ridge）、多项式岭回归和 XGBoost。针对这些模型，手动提取了均值、标准差、末值、差值和斜率等统计特征。
    *   **1D CNN：** 将窗口数据视为空间信号，通过三层卷积提取局部时间特征。
    *   **LSTM：** 采用单层 LSTM 捕捉长程时间依赖关系，仅利用最后一个时间步的隐藏状态进行预测。

### 3. 实验设计
*   **数据集：** 使用 NASA 的 **C-MAPSS** 基准数据集，重点分析了 **FD001**（单故障模式、单操作条件）和 **FD003**（双故障模式、单操作条件）子集。
*   **Benchmark（基准）：** 对比了 Zheng 等人（2017）提出的深度 LSTM 模型。
*   **评估指标：** 均方根误差（RMSE）、平均绝对误差（MAE）、决定系数（$R^2$）以及 **NASA 评分函数**（该函数对“高估 RUL”的惩罚远大于“低估”，更符合工业安全逻辑）。

### 4. 资源与算力
论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。但从模型描述（单层 LSTM、三层 CNN）和数据集规模（各 100 台发动机）来看，该实验对算力的需求较低，普通的消费级 GPU 或高性能 CPU 即可完成。

### 5. 实验数量与充分性
*   **实验覆盖：** 论文在两个不同复杂度的子集上进行了测试，并针对 CNN 和 LSTM 进行了**消融实验**（如对比不同层数、核大小、正则化方法）。
*   **深度分析：** 包含了**序列长度敏感性分析**（验证历史数据对预测的影响）和 **LSTM 隐藏状态可视化**（验证模型是否真正学到了退化趋势）。
*   **客观性：** 所有模型共享完全相同的预处理和验证集划分（80/20 比例），确保了对比的公平性。实验设计较为充分且客观。

### 6 主要结论与发现
*   **LSTM 表现最优：** 在原始序列模型中，单层 LSTM 的 RMSE（FD001: 14.93, FD003: 14.20）优于前人研究的深度 LSTM，证明了合理的学习率调度和架构比单纯增加深度更重要。
*   **XGBoost 的竞争力：** 在使用工程特征的情况下，XGBoost 在 FD003 上的 RMSE 达到 13.36，甚至超过了深度学习模型，说明非线性树模型在处理统计特征时极具优势。
*   **CNN 的保守性：** 1D CNN 在 FD001 上获得了最低的 NASA 评分（365），表明其预测结果更倾向于低估 RUL，这在安全敏感的应用中反而是一种优良特性。
*   **时间依赖性验证：** 实验证明 FD003（多故障）对历史数据的依赖性远高于 FD001，移除 5 个历史步会导致 FD003 的误差激增 48%。

### 7. 优点
*   **公平的基准测试：** 解决了以往研究中因预处理差异导致模型难以直接对比的问题。
*   **“简约而不简单”：** 证明了通过优化训练策略（如 ReduceLROnPlateau 调度器），简单的单层模型可以超越复杂的深度模型。
*   **多维度评估：** 不仅关注 RMSE，还深入分析了 NASA 评分函数下的安全性表现。

### 8. 不足与局限
*   **场景覆盖有限：** 实验仅限于 FD001 和 FD003（单操作条件），未涉及更复杂的 FD002 和 FD004（六种操作条件），这限制了结论在多工况环境下的普适性。
*   **特征工程依赖：** 经典模型的优异表现高度依赖于手动提取的统计特征，而深度学习模型在无需人工干预的情况下表现更稳健。
*   **缺乏算力数据：** 未提供训练效率对比，难以评估模型在嵌入式或边缘计算设备上的部署可行性。

（完）
